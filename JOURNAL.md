# JOURNAL

개발 과정의 의사결정, 막힌 지점, AI 활용을 날짜순으로 기록한다.

GitHub 이슈에는 목표, 작업 목록, 결정 요약만 적는다. 결정의 근거와 과정은 이 문서가 원본이다.

<!--
작성 규칙
- 날짜마다 "## Day N (YYYY-MM-DD)" 섹션을 만든다.
- 그 안에 이슈마다 "### #이슈번호 제목" 섹션을 만든다. 이슈 본문의 JOURNAL 링크는 이 섹션을 가리킨다.
- 한 이슈가 여러 날에 걸치면 날짜마다 같은 제목의 섹션을 이어서 만든다.
- 의사결정은 상황, 채택한 방식과 근거, 다른 방법과의 비교, 그렇게 판단한 이유 순으로 적는다.
- AI 활용은 무엇을 물었는지, 답을 수용·수정·거부했는지와 그 이유를 적는다.
- 완성된 결과만이 아니라 시행착오를 적는다. 해당 없는 항목은 지운다.
- 수행 내용은 한 문장으로 각각 적는다.

섹션 템플릿

### #N 제목

**수행 내용**
-

#### **의사결정**
- 상황:
- 채택:
  - 근거:
- 비교:
- 판단:

**막힌 점과 해결**
-

**테스트**
- 전략:
- 결과:

**AI 활용**
- 질문:
  - 처리: 수용 / 수정 / 거부
  - 이유:
-->

## Day 1 (2026-09-13)

### #2 기술 스택 결정

**수행 내용**
- 언어, 프레임워크, DB를 어떤 근거로 정했는가?

#### **의사결정**

언어

- 상황: 공급사 여러 곳을 동시에 호출하는 구조라 가상 스레드로 처리할 계획이고, 가상 스레드의 핀 문제를 방지하는 자바 버젼을 선택해야했다.
- 채택: Java 25
  - 근거: 핀 문제 해결은 JDK 24에 들어갔고, 25는 그것을 포함한 첫 LTS다. `synchronized` 를 쓸 일은 없지만 해결된 버젼을 쓰는게 안정적이다.
- 비교: Java 21 — LTS이고 가상 스레드는 있지만 핀 문제가 남아 있어 `synchronized`를 쓰는 라이브러리를 피하거나 `ReentrantLock`으로 바꾸는 부담이 생긴다.
- 판단: LTS이면서 핀 문제가 없는 버전은 25뿐이라 25를 골랐다.

프레임워크

- 상황: Spring Boot 3.x와 4.x 중 고르는 시점이었다.
- 채택: Spring Boot 4.1, Spring MVC
  - 근거: Boot 3.5의 OSS 지원이 2026-06에 끝났다. 
- 비교: Spring Boot 3.5 — 자료가 많고 생태계 호환이 안정적이지만 OSS 지원이 끝났다. 4.x는 신규 메이저라 springdoc 등 호환 확인 비용이 있다.
- 판단: 지원 주기가 결정적이었다. 호환 확인 비용은 시작 시점에 한 번 치르면 되지만, 지원이 끝난 라인은 프로젝트 내내 부담이 된다.

DB

- 상황: 통합 테스트에서 DB를 어떻게 띄울지 정해야 했다.
- 채택: H2를 실행과 테스트 모두
  - 근거: 
    - 실행 DB와 테스트 DB가 같다.
    - 격리 수준을 고려하지 않아도 되는 기획안이라고 생각하고, 가벼운 테스트 환경이 우선이라고 생각한다.
    - Docker 없이 통합 테스트가 돈다.
- 비교: Testcontainers + MySQL/Postgres — Docker가 필수이고 CI가 느려진다.
- 판단: H2로 선택한다. 운영 DB가 정해지고 쓰기/읽기 정합성 문제가 발생하는 상황일때 RDBMS를 선택할 것이다.

**AI 활용**
- 질문: Spring Boot 4 선택 근거로 무엇을 적을지 물었다.
  - 처리: 수정
  - 이유: Spring Boot 4 근거는 지원 주기, Java 25 지원, 코어 resilience, HTTP Service Client 네 가지를 받았다.

## Day 2 (2026-09-14)

### #8 통합 모델·매핑 엔티티 설계

**수행 내용**
- 요금·재고·실패 판정을 어떤 단위로 통합할지 정했다.
- 병렬 호출과 트랜잭션 경계에서 스레드와 커넥션이 어디에 묶이는지 정리했다.

#### **의사결정**

요금 표준

- 상황: A는 날짜별 금액 + 세액, B는 기간 총액만 준다.
- 채택: 숙박 전체 총액, 세금 포함 하나로 통일.
  - 근거: 
    - 고객이 비교 및 결제하는 값은 총액이고, 두 공급사가 공통으로 줄 수 있는 유일한 요금 단위다. 
    - 세금 별도로 보여주면 결제 단계에서 가격이 올라 보인다.
    - 실제 요금 정책상 세금을 포함해서 보여줘야 한다.
- 비교: 날짜별 단가 
  - B의 총액을 일수로 나누면 요일과 공휴일 차이를 무시한 값이 된다. 
  - B 총액은 기간 가격이라, 만약에 연박으로 인해 할인 정책이 반영된거면 1박씩 재조회한 가격과 다를 수 있다.
- 판단: 정확하지 않은 값을 보여주느니 빼는 쪽을 택했다. 잃는 것은 A의 날짜별 금액이다. 세금 정보를 안주는 공급사가 생겨도 다른 공급사 값으로 추정하지 않는다(같은 객실도 공급사마다 가격·조건이 다르다).

실패 판정

- 상황: A는 HTTP 상태 코드, B는 항상 200 + 본문 코드로 실패를 알린다.
- 채택: 두 어댑터 모두 HTTP 상태 → 본문 파싱 → 본문 코드 순으로 검사하고, 도메인에는 재시도 판단이 가능한 내부 분류만 넘긴다.
  - 근거: A도 4xx/5xx에 JSON 본문을 주고, B도 200이 아닌 응답이 올 수 있어 한쪽만 보는 검사는 불완전하다.
- 비교: A 코드를 B 코드에 1:1 매핑 — 공급사 코드 체계가 도메인에 새고, 재시도 여부를 정하는 기준이 안 된다.
- 판단: 분류 기준을 "재시도해도 되는가"로 잡았다. 400·401은 우리 쪽 문제라 재시도 금지, 429·503·타임아웃만 재시도 대상.

병렬 호출과 스레드

- 상황: MVC 위에서 WebClient로 A·B를 동시에 호출해야 한다. WebClient는 논블로킹이지만 MVC에서는 블로킹으로 결과를 받아야 하고, 그 지점에서 요청 스레드가 가장 느린 공급사만큼 묶인다.
- 채택: 공급사 호출은 가상 스레드 Executor에 태스크로 제출해 병렬로 돌리고, 톰캣 요청 스레드도 가상 스레드로 켠다.
  - 근거:
    - 스택 트레이스가 호출 흐름과 일치해 추적이 쉽다. 
    - 계층 규칙, `@Transactional`, `AppException`, `ApiControllerAdvice`가 모두 동기 전제다. `Mono`가 Business로 올라오면 트랜잭션·예외 처리를 리액티브용으로 한 벌 더 만들어야 한다.
    - 부분 실패 조립이 Future별 `try/catch`로 끝난다.
    - 기획안이 MVC 위에서 WebClient만 쓰는 구성을 명시적으로 허용한다.
- 비교:
  - 컨트롤러가 `Mono` 반환 — 요청 스레드를 즉시 놓지만 서비스 계층 전체에 리액티브가 스며든다.
- 판단: 결과 모으기만 동기로 바꾸면 WebClient의 타임아웃·재시도 제어는 그대로 쓰면서 나머지 코드가 전부 동기가 된다. 비용은 요청당 가상 스레드 1 + 배치 수(무시 가능), `.block()`을 이벤트 루프에서 호출하면 안 된다는 규칙, Executor로 넘어갈 때 ThreadLocal(MDC)이 안 따라간다는 점이다. `StructuredTaskScope`는 JDK 25에서도 preview라 쓰지 않는다.

트랜잭션 경계

- 상황: 매핑 조회와 공급사 호출이 한 메서드 안에 있다.
- 채택: 매핑 조회만 read-only 트랜잭션, 공급사 호출은 트랜잭션 밖, OSIV 비활성.
  - 근거: 가상 스레드는 스레드를 놓지 DB 커넥션을 놓지 않는다. 트랜잭션이 메서드 전체를 감싸면 공급사 응답을 기다리는 동안 커넥션이 풀에 안 돌아온다.
- 판단: 스레드 낭비(가상 스레드)와 커넥션 낭비(트랜잭션 분리)는 별개 문제라 둘 다 처리한다.

**AI 활용**
- 질문: 위 구조 초안을 주고 반박을 요청했다.
  - 처리: 수정
  - 이유: 총액·gross 기준과 트랜잭션 분리는 유지. "날짜별 가격은 재조회로 대체 가능", "세금 미제공 공급사는 다른 공급사 세금으로 계산" 근거가 틀려 폐기했다. N박 재고 판정, 배치 단위 실패, 타임아웃 상한, 매핑 미존재 처리는 빠져 있어 미결정 목록에 추가했다.
- 질문: WebClient가 논블로킹이면 가상 스레드가 필요 없는지, 마운트·언마운트가 어느 지점에서 일어나는지 물었다.
  - 처리: 수용
  - 이유: 블로킹 지점이 사라지는 게 아니라 `.block()`과 JDBC 대기로 옮겨갈 뿐이라는 설명을 받았다. 하나의 가상 스레드가 메서드 처음부터 끝까지 이어지고 두 대기 지점에서 캐리어를 놓는다.
- 질문: 병렬 호출 방식을 물었더니 `Mono.zip` + `.block()` 한 번을 권했다.
  - 처리: 거부
  - 이유: 팬아웃까지 리액티브로 가면 동기 계층 구조와 어긋나고 추적이 어려워진다. 타임아웃·재시도는 어느 쪽이든 WebClient 체인에 남는다는 점을 확인하고, 결과 모으기만 가상 스레드 Executor로 바꿨다.

## Day 3 (2026-09-15)

### #8 매핑 엔티티·리포지토리 구현

**수행 내용**
- `Stay`, `RoomType` 엔티티와 공통 상위 `BaseEntity`, 각각의 리포지토리를 구현했다.

#### **의사결정**

내부 식별자

- 상황: PK를 자동 증가 순번으로 할지 UUID로 할지 정해야 했다.
- 채택: UUID, 애플리케이션에서 생성(`@UuidGenerator`)
  - 근거: 순번 ID의 정렬 이점은 지금 범위(정렬·페이징)에 해당하지 않는다. 예약 흐름이 붙으면 열거 가능한 순번 ID가 다른 리소스를 추측하는 통로가 되므로 처음부터 불투명하게 간다.
- 비교: DB IDENTITY 순번 — 구현이 단순하고 인덱스 지역성이 좋지만, 나중에 불투명 ID로 바꾸려면 예약 흐름이 붙은 뒤라 비용이 더 크다.
- 판단: 안정성은 유일 제약(`supplier`+`stay_code`, `stay_id`+`room_type_code`)과 동기화 시 upsert가 이미 보장하므로, 순번의 이점을 포기해도 잃는 게 없다.

삭제 대신 비활성화

- 상황: 공급사 목록에서 숙소·객실이 빠졌을 때 레코드를 지울지 남길지 정해야 했다.
- 채택: `active` 플래그로 표시하고 물리 삭제하지 않는다(`markAsNonExist`).
- 근거: 공급사가 일시적으로 목록에서 뺐다가 다시 등장하는 경우 삭제 후 재생성하면 UUID가 바뀌어 이전 매핑과의 연속성이 끊긴다. 물리 삭제는 되돌릴 수 없다.
- 비교: 물리 삭제 — 테이블이 항상 최신 목록만 담아 조회는 단순해지지만, 재등장 시 새 UUID로 생겨 연속성이 끊기고 삭제 자체를 되돌릴 수 없다.
- 판단: 되돌릴 수 없는 삭제보다 플래그 하나가 싸다.

연관관계 매핑

- 상황: `RoomType`이 `Stay`를 참조하는 방식을 정해야 했다(`@ManyToOne` vs 순수 컬럼).
- 채택: JPA 연관 매핑 없이 `stayId`를 평범한 UUID 컬럼으로 두고, FK 제약은 DDL(`schema.sql`)에만 건다. cascade도 없다.
- 근거: 실제 접근 패턴이 `Stay`를 통해 `RoomType`을 지연 로딩하는 게 아니라, 리포지토리가 `findAllByStayIdIn`으로 이미 배치 조회한다. 연관 매핑은 N+1, 프록시 직렬화, `equals/hashCode` 문제를 끌고 들어오는데 그걸 상쇄할 만큼 쓰이지 않는다.
- 비교: `@ManyToOne(fetch = LAZY)` — 객체 그래프 탐색은 편해지지만 그렇게 쓰는 코드가 없어 이점 없이 복잡도만 는다.
- 판단: 실제 접근 패턴에 맞춰 가장 단순한 형태를 택했다.

공통 시각 컬럼

- 상황: `created_at`/`updated_at`은 흔한 패턴이지만, `last_synced_at`(목록 API에 마지막으로 등장한 시각)을 같은 방식으로 관리할 수 있는지가 애매했다.
- 채택: 셋 다 `BaseEntity`에 두되, 앞의 둘은 Spring Data JPA Auditing(`@CreatedDate`/`@LastModifiedDate`)에 맡기고, `last_synced_at`은 `@PrePersist`로 최초 값만 채운 뒤에는 도메인 메서드(`markSynced`)가 명시적으로 갱신한다.
- 근거: `updated_at`은 필드가 바뀌면 항상 갱신되어야 하니 Auditing이 맞다. 반면 `last_synced_at`은 "이번 동기화 주기에 등장했다"는 의미라 필드 값이 그대로여도(같은 이름으로 다시 sync) 갱신돼야 하고, 반대로 비활성화 시점에는 갱신되면 안 된다. Auditing 하나로는 이 둘을 구분할 수 없다.
- 비교: 세 컬럼 모두 Auditing에 맡기기 — 구현은 단순해지지만 비활성화 시점에 `updated_at`이 의미 없이 갱신되거나, `last_synced_at`이 "마지막으로 등장한 시각"이 아니라 "마지막으로 건드린 시각"이 되어버린다.
- 판단: 의미가 다른 시각을 같은 메커니즘으로 묶지 않았다.

공통 시각 컬럼 (변경)

- 상황: 위 방식으로 동기화를 구현하니 `last_synced_at`이 매번 바뀌어, 값이 그대로인 행까지 목록에 있는 기존 행 전부에 UPDATE가 나갔다. 그 결과 `updated_at`도 매 동기화마다 갱신돼 실제 변경 시각이라는 의미를 잃었다.
- 채택: 엔티티 `applyLatestInfo`가 DB 값과 다를 때만(이름·최대 인원 변경, 재활성화) 필드를 바꾸고, 같으면 건드리지 않는다. `last_synced_at`은 없애고 `created_at`·`updated_at`만 둔다.
  - 근거: 숙소·객실 정보는 자주 바뀌지 않으므로 매 동기화마다 모든 행을 쓰는 건 비효율이다. 바뀐 행만 쓰면 `updated_at`이 실제 변경 시각이 되고, 그러면 `last_synced_at`은 `updated_at`과 거의 같은 값이라 둘 이유가 없다.
- 비교: `last_synced_at`은 "마지막 등장 시각"으로 유지하고 벌크 UPDATE 한 번으로 갱신, 내용은 바뀐 행만 반영 — 등장 시각 정보는 지키지만 벌크 쿼리가 영속성 컨텍스트·Auditing을 우회해 순서와 정합성을 따로 챙겨야 한다.
- 판단: "마지막으로 목록에서 본 시각"을 쓰는 곳이 없어, 그 정보를 지키려고 쓰기 경로를 복잡하게 만들 이유가 없었다.

리포지토리 조회 메서드

- 상황: 검색 경로와 동기화 경로가 같은 테이블을 다른 조건으로 읽는다.
- 채택: `StayRepository`에 `findAllBySupplierAndActiveTrue`(검색용)와 `findAllBySupplier`(동기화용, 비활성 포함)를 따로 두고, `RoomTypeRepository`는 `findAllByStayIdIn` 하나만 둔다(active 조건 없음).
- 근거: 검색은 지금 팔 수 있는 숙소만 봐야 하니 `active=true` 필터가 필요하다. 동기화는 비활성 레코드도 다시 활성화될 수 있어 전체를 봐야 대조가 된다. `RoomType`은 검색에서도 active로 거르지 않는데, 실제로 어떤 객실을 팔지는 재고·요금 API 응답이 정하고 매핑 테이블은 코드→이름 번역 사전 역할만 하기 때문이다.
- 비교: `RoomType`에도 `findAllByStayIdInAndActiveTrue` 추가 — `Stay`와 대칭은 맞지만 지금 쓰는 곳이 없는 메서드가 된다.
- 판단: 실제로 쓰이는 조회만 만들고, 쓰이지 않는 대칭 메서드는 필요가 생기면 추가한다.

리포지토리 조회 메서드 (변경)

- 상황: 동기화를 구현하고 보니 검색용 `findAllBySupplierAndActiveTrue`를 부르는 곳이 없었다. 검색 기능은 아직 구현 전이다.
- 채택: `StayRepository`에는 동기화용 `findAllBySupplier`만 남기고 검색용 메서드는 제거했다. `RoomTypeRepository.findAllByStayIdIn`은 그대로 둔다.
  - 근거: 위에서 정한 "실제로 쓰이는 조회만 만든다"는 기준을 `Stay`에도 똑같이 적용했다.
- 판단: 검색에서 활성 숙소만 읽는다는 결정 자체는 유지하고, 메서드는 검색을 구현할 때 추가한다.

**AI 활용**
- 질문: 숙소·객실 동기화 로직 구현을 맡겼다.
  - 처리: 수정
  - 이유: AI는 목록에 있는 기존 행마다 이름·최대 인원·`active`·`last_synced_at`을 매번 다시 넣는 방식으로 짰다. 시각이 매번 바뀌어 값이 그대로인 행까지 전부 UPDATE가 나갔다. 기획 문서상 숙소·객실 정보는 자주 바뀌지 않으므로 매 동기화마다 모든 행을 쓰는 건 비효율이라 판단했고, 엔티티 `applyLatestInfo`가 DB 값과 다를 때만(이름·최대 인원 변경, 재활성화) 필드를 바꾸도록 고쳤다. 그러면 `last_synced_at`이 `updated_at`과 겹쳐 컬럼을 없애고 `updated_at`만 남겼다.

### #9 공급사 숙소 목록 동기화

**수행 내용**
- 공급사 설정을 yml 맵으로 바꾸고 공급사별 WebClient 빈을 구성했다.
- 공급사 A·B 숙소 목록 Client와 공통 호출 실행기(`SupplierHttpCaller`)를 구현했다.
- 기동 시·매일 04:00·수동 API로 숙소·객실 목록을 동기화하는 서비스와 진입점을 구현했다.
- 공급사 A·B를 흉내 내는 Mock 서버를 추가했다.
- 동기화 중 목록 항목 하나가 검증에 실패했을 때의 처리 방식을 정했다.
- 공급사별로 DB 저장 실패를 격리했다.
- 항목 검증 실패 로그에 첫 오류의 코드와 사유를 남겼다.
- 공급사 연동 코드를 `external/supplier`로, Mock 서버를 `mock/supplier`로 옮겼다.
- B 본문 결과 코드 매핑을 전용 enum `BResultCode`로 바꿨다.
- 로그 메시지 형식을 통일하고, 빈 목록으로 동기화를 건너뛸 때도 로그를 남겼다.

#### **의사결정**

동기화 쓰기 배치

- 상황: 동기화에서 새 숙소·객실은 `saveAll`로, 바뀐 행은 변경 감지로 저장한다. 배치 설정이 없으면 커밋 때 INSERT·UPDATE가 한 건씩 나가, 숙소가 수천 개면 쿼리 왕복도 수천 번이 된다.
- 채택: JDBC 배치 `hibernate.jdbc.batch_size=50`, `order_updates=true`. `order_inserts`는 켜지 않는다.
  - 근거: `saveAll` 자체는 묶어 보내지 않고 엔티티마다 `persist`할 뿐이라 설정이 있어야 배치가 된다. ID를 앱에서 만들어(`@UuidGenerator`) INSERT 전에 ID를 알 수 있으므로 INSERT도 배치된다. 크기 50은 잠정값이다. 동기화는 공급사 목록 전체를 한 트랜잭션에서 커밋 때 쓰므로 공급사 조회 단위(50)와는 관계가 없다. 변경 감지 UPDATE는 `Stay`·`RoomType`이 섞여 배치가 끊길 수 있어 `order_updates`로 엔티티별로 모은다.
- 비교: 배치 없음 — 실패한 행이 로그로 바로 보이지만 쿼리 왕복이 행 수만큼 든다. `order_inserts` 추가 — 연관 매핑 없이 `stay_id` 컬럼만 있어 Hibernate가 FK 순서를 모르므로 객실 INSERT가 숙소보다 앞설 위험이 있고, 숙소를 먼저 저장해 이미 종류별로 모여 있어 얻는 게 없다. 배치 크기 100 — 왕복은 더 줄지만 기준으로 삼을 근거가 없었다.
- 판단: ID 생성 방식 덕에 코드 변경 없이 설정만으로 쿼리 왕복을 크게 줄일 수 있어 채택했다.
- 추후: 지금 H2는 앱 안에서 도는 DB라 네트워크 왕복이 없어, 배치 크기를 바꿔도 효과가 작다. 서버형 DB로 옮기면 크기를 다시 정한다. 흔히 100~200을 쓴다. 전체 건수가 아니라 드라이버의 멀티 row 재작성 지원, 네트워크 지연, 행 크기·패킷 한도를 보고 정한다.

동기화 트랜잭션 범위

- 상황: 숙소 동기화(`StayManager.sync`)와 객실 동기화(`RoomTypeManager.sync`)가 각각 `@Transactional`이고, `StaySyncService`가 공급사 목록을 돌며 공급사마다 숙소 동기화를 호출한다.
- 채택: `RoomTypeManager.sync`는 기본 전파(`REQUIRED`)로 숙소 트랜잭션에 참여하고, 트랜잭션은 공급사마다 분리된다.
  - 근거:
    - 한 공급사의 숙소와 객실은 같은 트랜잭션에서 함께 커밋되거나 함께 롤백된다.
    - `StaySyncService`에는 트랜잭션이 없고 `StayManager.sync` 호출이 공급사마다 새 트랜잭션을 연다. 한 공급사의 동기화가 롤백돼도 이미 커밋된 다른 공급사 결과는 남는다.

공급사 설정과 WebClient 구성

- 상황: 공급사 C가 들어올 때 공통 설정 클래스(`SupplierProperties`의 `a`·`b` 필드, `WebClientConfig`)를 매번 고쳐야 했다.
- 채택: 설정은 yml `supplier.endpoints`의 `Map<Supplier, SupplierEndpoint>`로 바꾸고, WebClient는 공급사별 빈으로 유지한다. 빈은 Boot가 구성한 `WebClient.Builder`를 복제해 만든다.
  - 근거: 공급사 목록과 WebClient 구성이 설정 클래스 한곳에서 보이고, 특정 공급사만 헤더·타임아웃을 달리하기 쉽고, 테스트에서 Client에 WebClient를 바로 넣을 수 있다. Boot Builder를 써야 나중에 계측을 붙일 수 있다.
- 비교: 팩토리 빈 하나를 두고 Client가 생성자에서 자기 WebClient를 만드는 방식 — C를 추가해도 설정 클래스를 안 고치지만, 의존성이 Client마다 흩어지고 공급사별 조정에 팩토리 옵션이 필요하다.
- 판단: 두 방식 모두 기동 시 한 번 만들어 재사용해 실행 성능 차이는 없다. C 추가 시 빈 메서드 하나를 더하는 비용보다 명시성이 낫다고 봤다.

공급사 호출 구조

- 상황: A·B Client에 요청 실행·타임아웃·`block()`·예외 변환 흐름이 거의 똑같이 복사돼 있어, C가 오면 또 복사하게 된다.
- 채택: 공통 실행기 `SupplierHttpCaller`를 두고 Client가 조합해서 쓴다. Client는 요청 조립과 응답 판정만 맡는다.
  - 근거: 공통 흐름이 한곳에 모여 C는 판정만 쓰면 된다. 나중에 모니터링·재시도를 붙일 자리도 이 실행기 하나다.
- 비교: 추상 베이스 클래스 — 익숙하지만 A(`Class`)와 B(제네릭 봉투)의 응답 타입 차이 때문에 템플릿 메서드 시그니처가 복잡해지고 상속 한 줄에 묶인다. 데코레이터로 기존 Client 감싸기 — 기존 코드를 안 고치지만 메서드 단위로만 보여 묶음 호출 일부 실패를 구분할 수 없다.
- 판단: 한곳에서 관리할 수 있는 조합을 택했다.

실패 판정 위치

- 상황: 기존 A는 `onStatus`에서 `Mono`로 실패를 만들었고, B는 `block()` 뒤 본문 코드를 봐서 판정 위치가 두 곳으로 갈렸다. 스레드를 찍어 보니 `onStatus` 핸들러는 가상 스레드가 아니라 Netty 이벤트 루프에서 돌았다.
- 채택: 응답을 `exchangeToMono(toEntity(String))`로 상태·본문 문자열째 받아 `block()`하고, 판정은 호출한 스레드의 동기 코드에서 한다.
  - 근거: 서비스가 MVC 동기 구조라 외부 호출 판정도 동기 코드로 통일한다. 가상 스레드를 쓰므로 `block()` 대기 비용은 작다. A·B 판정이 같은 모양이 되고, 상태 코드·본문 코드·원본 본문을 한곳에서 볼 수 있어 로그·메트릭을 구체적으로 붙이기 쉽다. 이벤트 루프가 아니라 호출 스레드라 MDC도 유지된다.
- 비교: `onStatus` + `Mono` 유지 — 역직렬화와 본문 해제를 Spring이 맡고 재시도를 체인 안 `.retryWhen()`으로 걸 수 있지만, 판정 위치가 갈리고 에러 처리가 리액티브 문법으로 남는다. RestClient로 교체 — `Mono`가 아예 없지만 WebClient 결정과 충돌한다.
- 판단: 동기 판정으로 통일했다. 대가로 JSON 파싱을 직접 하고, 재시도를 붙일 때 `.retryWhen()`이 아니라 실행기 단위로 건다.

응답 본문 수신과 버퍼 한도 (고려 사항)

- 상황: 성공이면 목록 DTO, 실패면 에러 DTO로 읽어야 해 수신 시점에 타입을 정할 수 없다. 본문을 문자열로 받으면 전체가 메모리에 올라가는데, WebClient는 기본 256KB까지만 모은다. 숙소 약 3,000개 목록(약 390KB)으로 실험하니 DTO로 받든 문자열로 받든 실패했고, 기존 코드에서는 이 실패가 `INTERNAL/200`으로 잘못 분류됐다.
- 채택: 본문은 문자열로 받고, 한도는 기본값을 유지하되 초과를 `MALFORMED/BODY_TOO_LARGE`로 분류해 원인이 드러나게 한다.
  - 근거: 실제 목록 데이터와 크기를 모른다.
- 비교: 지금 한도를 올리기 — 근거 없는 숫자가 된다. 응답을 스트리밍으로 파싱 — 큰 목록에도 메모리가 일정하지만, 판정 전에 타입을 정해야 해 동기 판정 구조와 맞지 않고 복잡해진다.
- 판단: 결정하지 않고 `decisions.md` 고려 사항에 메모했다. 실제 목록 크기를 알게 되면 한도를 정한다.

응답 판정 세부 규칙

- 빈 본문과 깨진 본문을 나눈다: 본문이 없거나 JSON `null`이면 `EMPTY_BODY`, 파싱 실패면 `UNPARSEABLE`. Jackson은 빈 문자열에도 파싱 예외를 던져 그대로 두면 둘이 섞인다. 운영에서 "안 왔다"와 "이상하게 왔다"는 원인이 달라 구분했다.
- A의 실패 본문이 JSON이 아니면(게이트웨이 HTML 등) 원본 코드는 상태 숫자로 대신한다. 실패 분류는 이미 상태 코드로 정해졌으므로 본문 때문에 판정이 흔들리지 않게 했다.
- 전송 오류 변환 순서는 타임아웃 → 버퍼 한도 → 연결 오류 → 그 외(`UNEXPECTED`). 타임아웃·버퍼 한도는 연결 예외 안에 감싸여 올 수 있어 원인 체인을 먼저 본다. 예전에는 예상 못한 오류가 전부 `UNPARSEABLE`로 묶여 파싱 문제로 오인될 수 있어 `UNEXPECTED`로 분리했다.
- 값의 옳고 그름은 VO가 판단한다: 응답 record는 null 숫자를 0으로 넘기기만 하고, 최대 인원 1 미만 검사는 `SupplierRoomType` 생성자 한곳에서 한다(`decisions.md` 도메인 검증).

신규 공급사 추가 절차

공급사 C를 붙일 때 고치는 곳과 고치지 않는 곳이다.

| 순서 | 위치 | 할 일 |
|------|------|------|
| 1 | `supplier/Supplier` | enum 값 추가. 이름은 16자 이하(`stay.supplier VARCHAR(16)`, 엔티티 `length = 16`)이고 yml 키와 같아야 한다 |
| 2 | `application.yaml` | `supplier.endpoints.C`에 `base-url`, `api-key`. 빠지면 기동이 멈춘다 |
| 3 | `config/supplier/WebClientConfig` | `supplierC` 빈 메서드 추가(`build(builder, properties, Supplier.C)`) |
| 4 | `supplier/c/response` | 응답 record. 접두어 `C*`. 빠질 수 있는 숫자는 `Integer` 같은 래퍼 타입. `toSupplierStay()`로 VO 변환까지 맡는다 |
| 5 | `supplier/c/SupplierCClient` | `@Component`, `implements SupplierClient`. 아래 체크리스트대로 작성. 본문 코드가 있으면 같은 패키지에 `CResultCode` enum |
| 6 | `mock-supplier` 모듈의 `MockSupplierController` | 로컬 실행용 C 응답 추가 |
| 7 | 테스트 | `supplier/c`에 MockWebServer로 정상·실패 분류 케이스 |

`SupplierCClient` 작성 체크리스트

- 생성자는 직접 쓴다: `@Qualifier("supplierC") WebClient`, `SupplierHttpCaller`. Lombok 생성자는 필드의 `@Qualifier`를 파라미터로 옮기지 않는다.
- `private static final Supplier SUPPLIER = Supplier.C`를 두고 예외·실행기에 넘긴다.
- `fetchStays()`는 `caller.call(SUPPLIER, webClient.get().uri(...), (status, body) -> read(status, body, 타입))`.
- `read`는 C가 실패를 어떻게 알리는지부터 확인하고 작성한다.
  - HTTP 상태로 알리면 A처럼 `status.isError()` → `SupplierErrors.classify(status)`.
  - 항상 200에 본문 코드로 알리면 B처럼 파싱 뒤 코드 검사. 봉투가 제네릭이면 `TypeReference`로 파싱한다.
  - 둘 다 쓰면 HTTP 상태 → 본문 파싱 → 본문 코드 순서(`decisions.md` 실패 판정).
- C 고유 코드는 C 패키지 안에서만 `SupplierFailureType`으로 바꾼다(본문 코드는 B의 `BResultCode`처럼 전용 enum). 기준은 "재시도해도 되는가"다(우리 쪽 문제면 `BAD_REQUEST`·`AUTH`, 기다리면 나아질 수 있으면 `RATE_LIMITED`·`UNAVAILABLE`). 원본 코드는 `code` 문자열로 남긴다.
- 성공 본문은 `caller.parse`로 읽는다. 빈 본문·파싱 실패는 실행기가 분류한다.
- 목록 응답 고유 검사(목록 필드 null 등)는 `fetchStays`에서, DTO → `SupplierStay`·`SupplierRoomType` 변환은 Client 안에서 한다. DTO를 Client 밖으로 내보내지 않는다.

고치지 않는 곳: `StaySyncService`(Client 목록을 주입받아 돌기만 한다), `StayManager`·`RoomTypeManager`·`schema.sql`(공급사를 값으로만 다룬다), `SupplierHttpCaller`·`SupplierErrors`(새로운 종류의 전송 오류가 생길 때만), `SupplierProperties`.

동기화 항목 오류

- 상황: 공급사 목록 응답 100건 중 1건만 검증(`INVALID_SUPPLIER_STAY`)에 걸려도 `SupplierStay` 변환 스트림이 중단되고, `StaySyncService`가 그 공급사 동기화 전체를 건너뛴다. 정상인 99건도 반영되지 않는다. 처음에는 이 에러 처리가 요금·재고 검색에도 똑같이 적용된다고 생각했는데, 확인해 보니 VO 검증은 목록 동기화 경로에만 있고, 검색 경로는 이미 항목 단위로 빼기로 정해져 있었다(매핑 없는 객실, 날짜 누락). 그래서 정해야 할 대상은 목록 동기화뿐이었다.
- 채택: 한 항목이라도 검증에 실패하면 그 공급사 동기화 전체를 건너뛰고 기존 DB 값을 유지한다(현재 코드 그대로).
  - 근거: 요구사항상 숙소 목록은 자주 바뀌지 않는다. 다음 주기(매일 04:00)에 반영되므로 최대 하루 늦어져도 괜찮다. 반면 응답 오류 때문에 판매 중인 숙소 하나가 검색에서 사라지는 건 말이 안 된다.
- 비교:
  - 잘못된 항목만 빼고 나머지는 반영, 빠진 항목은 "목록에 없음"으로 처리 — 구현은 단순하지만 빠진 숙소가 `markAsNonExist()`로 비활성화돼 검색에서 사라진다. 필드명 변경 같은 구조적 오류로 전 항목이 깨지면 공급사 숙소 전체가 비활성화될 수 있다.
  - 잘못된 항목만 빼고, 빠진 코드는 "모름"으로 보고 기존 값 유지 — 정상 항목은 반영되고 비활성화도 없지만, `StayManager.sync`에 건너뛴 코드 개념을 추가해야 하고 stayCode 자체가 빈 항목에는 적용할 수 없다. 일부만 반영되니 공급사 데이터 이상도 조용히 묻힌다.
  - 위 방식에 "일정 비율 이상 깨지면 전체 건너뛰기" 추가 — 개별 오류와 구조적 오류를 둘 다 다루지만, 기준 비율에 숫자 근거가 없다(매핑 없는 객실에서 임계값을 채택하지 않은 이유와 같다).
- 판단: 검색과 동기화는 항목을 뺐을 때의 결과가 다르다. 검색은 그 응답에서만 안 보이고 다음 요청에서 다시 조회하지만, 동기화는 DB에 남고 목록에서 빠지면 비활성화된다. "하루 늦게 반영"과 "멀쩡한 숙소가 사라짐" 중 뒤쪽이 더 나쁘고, 목록 데이터가 잘 바뀌지 않는다는 요구사항 때문에 앞쪽 비용은 작다. 그래서 복잡도를 늘려 부분 반영할 이유가 없다고 봤다. 빈 목록을 응답 이상으로 보고 건너뛰는 기존 결정과도 방향이 같다.
- 남은 것: 실패를 알아채는 수단(지금은 warn 로그뿐, 모니터링 보류와 연결). 목록에 같은 코드가 두 번 오면 `StayManager`·`RoomTypeManager`가 첫 건만 쓰고 뒤 건은 로그 없이 건너뛴다(`decisions.md`도 이 동작 기준으로 맞췄다).

동기화 저장 실패 격리

- 상황: `StaySyncService`에서 `StayManager.sync` 호출이 `try` 밖에 있어, 한 공급사 저장 중 예외가 나면 `syncAll` 반복이 끊겨 뒤 공급사가 동기화되지 않았다.
- 채택: 공급사마다 저장 예외를 잡아 error 로그(예외 포함)를 남기고 다음 공급사로 넘어간다. DB 저장 재시도는 넣지 않는다.
  - 근거: 한 공급사의 DB 문제가 다른 공급사 동기화를 막을 이유가 없다. 트랜잭션이 공급사마다 분리돼 있어 실패한 공급사만 롤백된다.
- 비교: `@Retryable`로 저장 재시도 — 락 타임아웃·커넥션 대기 같은 일시적 오류는 스스로 복구되지만, 제약 위반 같은 결정적 오류는 같은 실패만 반복한다. 재시도를 다 써도 예외는 올라가므로 격리는 따로 필요하다. 시도마다 새 트랜잭션을 열어야 해 `@Transactional`과의 프록시 순서도 챙겨야 하고, 지금은 앱 내장 H2라 일시적 오류가 드물다.
- 판단: 격리만 먼저 한다. 동기화는 매일 돌고 수동 API도 있어 한 번 실패하는 비용이 작다. 재시도는 미결정(재시도·서킷 브레이커)과 함께 정한다.

항목 오류 로그

- 상황: 항목 검증에 실패하면 로그에 `supplier`만 남아, 어떤 숙소·객실이 왜 틀렸는지 알 수 없었다. 공급사에 문의하거나 원인을 찾을 근거가 없다. 또 첫 오류에서 변환이 멈춰 잘못된 항목이 몇 개인지도 알 수 없다.
- 채택: 공급사별 첫 오류만 로그에 남기고, 그 오류의 코드와 사유를 VO가 `AppException` data에 담는다(`stayCode=H1, 숙소 이름 없음`, `roomTypeCode=R1, 최대 인원 1 미만 (0)`).
  - 근거: 한 항목이라도 틀리면 공급사 전체를 건너뛰므로, 잘못된 항목 개수를 세도 처리가 달라지지 않는다. 검증을 VO 한곳에서 하므로 사유도 VO가 가장 정확히 안다.
- 비교:
  - 끝까지 검사해 잘못된 항목 수 세기 — 개별 오류인지 구조적 오류인지 보이지만, 전체를 건너뛰는 결정에서는 쓸 곳이 없다.
  - VO는 사유만, Client가 숙소 코드를 붙여 다시 던지기 — 어느 숙소의 어느 객실인지 다 보이지만, A·B Client 모두에 감싸는 코드가 들고 예외 생성자도 늘어난다.
- 판단: 수정이 적은 VO 방식을 택했다. 대가로 객실 오류는 객실 코드만 보이고 숙소 코드는 안 보인다. 여러 항목이 틀려 있으면 하나씩 고칠 때마다 다음 동기화에서 다음 오류가 드러난다. data는 `ApiControllerAdvice`가 응답 본문에 담으므로, 이 예외가 API 경로로 새면 공급사 코드가 노출된다는 점을 알고 선택했다(지금은 동기화 경로에서만 잡힌다).

로그 형식 통일

- 상황: 리뷰에서 동기화 로그가 기존 `ApiControllerAdvice`의 `[카테고리 : 상세내용]: key=value | key=value` 형식과 달리 문장형(`숙소 동기화 완료. supplier=..., created=...`)이라는 지적을 받았다. 또 동기화를 건너뛰는 사유 중 빈 목록만 로그가 없어 원인을 추적할 수 없었다.
- 채택: 모든 로그를 `[카테고리 : 상세내용]: key=value | key=value`로 통일하고 `CLAUDE.md`에 규칙으로 적었다. 레벨은 정상 `info`, 예상된 실패 `warn`, 시스템 오류 `error`. 빈 목록 건너뜀에 `[숙소 동기화 : 빈 목록]` warn 로그를 추가했다.
  - 근거: 카테고리 접두어와 `|` 구분이 같으면 로그 검색·집계 규칙을 하나로 쓸 수 있다. 빈 목록은 응답 이상으로 보고 건너뛰기로 한 경우라 다른 건너뜀 사유와 같이 warn으로 남긴다.
- 판단: 형식은 이미 쓰고 있던 것을 기준으로 삼고, 앞으로 흔들리지 않게 규칙 파일에 고정했다.

공급사 코드 매핑 위치

- 상황: B의 `resultCode`를 `SupplierBClient` 안의 `switch` 문자열로 `SupplierFailureType`에 매핑하고 있었다. 에러를 전역 `ErrorType`에서 한꺼번에 다뤄야 하지 않나 싶었다.
- 채택: `ErrorType`과 합치지 않고, B 전용 enum `BResultCode`를 B 패키지에 둔다.
  - 근거: `ErrorType`은 우리 API를 호출하는 쪽에 내려줄 HTTP 상태·메시지다. 공급사가 401을 준 건 우리 서버 설정 문제라 사용자에게 401을 주면 틀린 응답이 되고, 공급사 실패는 대부분 에러 응답이 아니라 검색 `failures[]`나 동기화 로그로 처리된다. 합치면 공급사 코드가 전역 enum에 섞여 공급사가 늘 때마다 전역 예외를 고쳐야 한다.
- 비교: `switch` 유지 — 짧고 한눈에 보이지만 코드 문자열이 메서드 안에 흩어진다. `ErrorType`에 매핑 — 한곳에서 보이지만 위 이유로 역할이 섞인다.
- 판단: 공급사 코드는 그 공급사 어댑터만 아는 지식이라 어댑터 안에 두되, 문자열을 enum 하나에 모았다. 코드가 없으면 `MALFORMED`, 모르는 코드는 `INTERNAL`로 둔다.

외부 연동 패키지

- 상황: `stay/implement`에 DB를 다루는 Manager와 HTTP 호출·JSON 파싱·전송 오류 변환 같은 외부 연동 코드가 섞여 있었고, 공급사 관련 코드가 `config/supplier`, `stay/implement`, `stay/vo`로 흩어져 있었다. Mock 서버 패키지 이름이 `supplier`라 실제 연동 코드와 헷갈렸다. 경계가 이상하다고 느꼈다.
- 채택: 공급사 연동 코드를 최상위 `external/supplier`에 통째로 모은다(인터페이스, 실행기, 예외·실패 분류, 설정, 입력 모델). 공급사별 Client·코드 매핑·DTO는 `a/`·`b/`에 둔다. 입력 모델(`Supplier`·`SupplierStay`·`SupplierRoomType`)도 `external`로 옮긴다. Mock은 `mock/supplier`로 이름을 바꾼다.
  - 근거: 공급사 관련 코드가 한곳에 모여 찾기 쉽고, 다른 도메인이 공급사를 쓰게 돼도 그대로 공유된다. 입력 모델을 `stay/vo`에 남기면 `stay.business → external → stay.vo`로 두 최상위 패키지가 서로를 참조하는 순환이 생긴다.
- 비교:
  - `stay/implement/supplier` 아래로만 분리 — 계층 규칙을 안 바꿔 변경이 가장 작지만, 외부 호출이 여전히 도메인 안에 있고 설정은 `config`에 떨어져 있다.
  - 구현만 `external`로, `SupplierClient` 인터페이스·예외·실패 분류는 도메인에 두는 의존성 역전 — 도메인이 HTTP를 전혀 모르고 경계가 가장 뚜렷하지만, 공급사 코드가 두 곳으로 나뉘고 규칙이 늘어난다.
  - 입력 모델을 `stay/vo`에 두고 순환 감수 — 도메인 모델이 도메인에 남지만, 나중에 `external`을 모듈로 떼거나 순환 검사를 걸면 다시 옮겨야 한다.
- 판단: 한곳에 모으는 쪽을 택하고, 순환 대신 한 방향 의존을 택했다. 대가로 Business·Manager·엔티티(`Stay.supplier`)가 `external`을 import하고, "Business는 Implement만 참조" 규칙에 "도메인은 `external`을 참조할 수 있다"가 더해졌다. `architecture.md`·`forbidden.md`·`review.md`·`test-conventions.md`를 새 구조로 고쳤다.

판단이 필요한 항목

구현 중 보였지만 아직 정하지 않은 것이다. 선택지와 트레이드오프만 적는다.

1. 목록 항목 하나가 잘못됐을 때 (결정: 위 "동기화 항목 오류")
   - 현재: 한 객실이라도 VO 검증에 걸리면 `AppException`으로 그 공급사 동기화 전체를 건너뛴다.
   - 선택지: 그대로 둔다 — 이상한 응답이 섞인 목록을 반영하지 않아 안전하지만, 한 건 때문에 나머지 정상 숙소도 갱신되지 않는다. 문제 항목만 버리고 경고 로그 — 나머지는 반영되지만 공급사 쪽 데이터 이상이 조용히 묻힐 수 있다.
2. 동기화 실패 로그에 원인 예외가 없다
   - 현재: 저장 실패·스케줄 실행 실패는 예외를 포함한 error 로그, 항목 검증 실패는 코드와 사유(data)를 남긴다. 목록 조회 실패 warn 로그만 supplier·type·code를 남겨 `UNEXPECTED`·`UNPARSEABLE`의 실제 원인을 볼 수 없다.
   - 선택지: 예외를 함께 로깅 — 원인이 보이지만 흔한 실패(타임아웃)에도 스택 트레이스가 쌓인다. 특정 분류(`UNEXPECTED`·`UNPARSEABLE`)만 예외를 남긴다 — 로그는 가볍지만 규칙이 하나 는다.
3. 타임아웃이 두 겹이다
   - 현재: 커넥터 읽기 타임아웃 3s와 `Mono.timeout` 3s. 값이 같으면 구독 시점부터 재는 `Mono.timeout`이 항상 먼저 터진다(실험 확인).
   - 선택지: 그대로 — 동작에 문제는 없다. 읽기 타임아웃을 없애거나 다른 값으로 — 역할이 명확해진다. 읽기 타임아웃은 패킷 사이 간격, `Mono.timeout`은 전체 시간이다.
4. B의 `resultCode`가 없을 때 원본 코드가 `null`로 남는다
   - 선택지: 그대로 — 실제로 코드가 없었다는 사실 그대로다. `MISSING_RESULT_CODE` 같은 값으로 채운다 — 로그·메트릭에서 읽기 쉽다.
5. 502·504가 `INTERNAL`로 분류된다
   - 현재: `UNAVAILABLE`은 503뿐이다. 재시도 여부를 이 분류로 정하기로 했으므로 재시도를 붙일 때 영향이 있다.
   - 선택지: 502·504도 `UNAVAILABLE` — 게이트웨이 일시 장애를 재시도 대상에 넣는다. 그대로 — 공급사 내부 오류와 같이 보고 재시도하지 않는다.
6. DB 단계 예외가 공급사별로 격리되지 않는다 (결정: 위 "동기화 저장 실패 격리")
   - 현재: `StayManager.sync`에서 예외가 나면 `syncAll` 반복이 끊겨 뒤 공급사가 동기화되지 않는다. 스케줄러는 예외를 잡아 기동은 계속된다.
   - 선택지: 공급사마다 `try/catch` — 한 공급사의 DB 문제가 다른 공급사를 막지 않지만, 제약 위반 같은 코드 결함이 로그로만 남는다. 그대로 — 결함이 크게 드러나지만 영향 범위가 넓다.
7. 수동 동기화 API가 공급사가 전부 실패해도 201을 준다
   - 저장 실패 격리 뒤로는 DB 저장 실패도 201이다(전에는 500). 실패는 잡혀 로그에 남지만 호출자는 결과를 모른다. 재시도를 정할 때 함께 다시 본다.
   - 선택지: 그대로 — 트리거 역할만 한다. 공급사별 결과(성공·건너뜀·사유)를 응답에 담는다 — 호출자가 결과를 알 수 있지만 응답 모델이 늘어난다.
8. 재시도·서킷 브레이커 (`decisions.md` 미결정)
   - 선택지: Spring Framework 7 코어(`@Retryable`, `RetryTemplate`) — 의존성 없이 재시도만, 서킷 없음, 프록시 기반이라 같은 클래스 안 호출에는 안 걸린다. Resilience4j — 재시도·서킷·레이트 리미터·메트릭 연동이 있지만 의존성이 늘고 Boot 4 전용 starter 여부를 확인해야 한다(없으면 코어 모듈로 직접 구성). 도입 안 함.
   - 붙일 때 정할 것: 적용 위치는 `SupplierHttpCaller.call` 단위(판정 뒤라 B의 본문 코드 실패도 보인다). 인스턴스는 공급사별로 나눠 한 공급사 장애가 다른 공급사 서킷을 열지 않게 한다. 서킷이 `BAD_REQUEST`·`AUTH`를 실패로 셀지. 서킷 open 예외(`CallNotPermittedException`)를 `UNAVAILABLE/CIRCUIT_OPEN`으로 바꾸지 않으면 `UNEXPECTED`로 떨어진다. 검색은 전체 타임아웃 5s·응답 3s라 재시도 1회도 전체 타임아웃을 넘길 수 있고, 새벽 동기화는 넉넉히 재시도할 수 있어 정책을 나눌지.
9. 공급사별 모니터링(성공률·응답 지연·타임아웃 비율) (보류)
   - 기본 `http.client.requests`만으로는 부족하다: B는 실패해도 200이라 성공으로 잡히고, Mock에서 A·B가 같은 호스트라 구분이 안 되며, 파싱 실패·전체 타임아웃 초과는 HTTP 단계에 없고, 재시도가 붙으면 시도마다 센다.
   - 설계안: `SupplierHttpCaller.call`을 감싸는 Timer `supplier.call`(태그 `supplier`, `operation`, `outcome`, `failure_type`, `reason`=TIMEOUT·CONNECTION·HTTP·BODY_CODE·PARSE 등). 원본 `code`는 값 종류를 통제할 수 없어 태그가 아니라 로그로만. 타임아웃 비율은 `failure_type`(`UNAVAILABLE`)만으로는 연결 실패와 섞여 `reason` 태그가 필요하다. 지연 히스토그램 경계는 0.5s·1s·3s(응답)·5s(전체). 동기화는 공급사별 마지막 성공 시각 게이지를 둬 며칠째 갱신이 안 되는 상황을 잡는다.
   - 주의: 전체 타임아웃 초과로 취소된 태스크는 태스크 안 타이머가 기록되지 않을 수 있어 결과를 모으는 쪽에서 기록한다. 가상 스레드 Executor로 넘어가면 MDC가 따라가지 않아 컨텍스트 전파가 필요하다.
   - 노출·알림(구축 시): actuator + Prometheus 레지스트리. 알림 임계값(성공률, p95, 타임아웃 비율, 마지막 동기화 경과)은 근거 수치가 없어 정하지 않았다.

**AI 활용**
- 질문: C를 쉽게 추가할 수 있게 구조 변경안을 물었다.
  - 처리: 수정
  - 이유: AI는 설정 Map, WebClient 팩토리, 공통 실행기를 제안했다. 설정 Map과 조합 실행기는 받아들이고, WebClient는 팩토리 대신 공급사별 빈을 택했다.
- 질문: 가상 스레드를 쓰면 에러 응답을 `Mono`가 아니라 바로 받을 수 있는지 물었다.
  - 처리: 수용
  - 이유: `onStatus` 핸들러가 Netty 스레드에서 돌아 `Mono`가 필요하다는 것을 스레드 출력으로 확인했고, 응답 전체를 받은 뒤 동기로 판정하는 방식을 택했다.
- 질문: `SupplierHttpCaller`의 흐름을 설명받은 뒤, 목록 100건 중 1건이 실패하면 바로 멈추는지 물었다.
  - 처리: 수용
  - 이유: AI가 `StaySyncService`·`StayManager`·`SupplierStay`·`StaySyncScheduler`를 읽고 경우를 셋으로 나눴다(호출 실패, 항목 검증 실패, DB 저장 실패). 공급사 호출이 항목마다가 아니라 목록 한 번이라는 점, 항목 하나 때문에 정상 99건이 버려진다는 점, DB 예외는 뒤 공급사까지 막는다는 점을 코드로 확인했다.
- 질문: 항목 오류 처리를 어떤 기준으로 골라야 하는지 힌트를 달라고 했다.
  - 처리: 수정
  - 이유: AI는 결론 없이 기준 6가지(틀림·오래됨·없음 중 무엇이 나쁜가, 항목 독립성, 개별·구조적 오류, 빠진 항목을 없음·모름 중 무엇으로 볼지, 실패 인지, 기존 결정과의 일관성)와 선택지 4개를 제시했다. 검색에도 같은 처리가 적용된다는 내 전제는 AI가 코드와 `decisions.md`로 바로잡았다. 이 중 첫 번째 기준에 요구사항(목록은 잘 바뀌지 않음)을 대입해 직접 결정했고, 나머지 기준은 전체 건너뛰기를 택하면서 따질 필요가 없어졌다.
- 질문: DB 저장 실패를 `@Retryable` 같은 재시도로 풀면 되는지 물었다.
  - 처리: 수정
  - 이유: AI는 재시도가 일시적 오류에만 효과가 있고 결정적 오류는 반복 실패하며, 재시도를 다 써도 뒤 공급사가 막히는 문제는 남는다고 답했다. DB 저장은 따로 보는 게 맞다고 판단해 재시도 없이 격리만 먼저 적용했다. 격리하면 수동 동기화 API가 저장 실패에도 201을 준다는 점도 짚었는데, 실패를 건너뛰는 구조라 지금은 로그로만 두고 재시도를 정할 때 함께 다시 보기로 했다.
- 질문: 잘못된 항목 개수 같은 로그로 데이터 오류를 파악하면 되는지 물었다.
  - 처리: 수정
  - 이유: AI는 지금 구조가 첫 오류에서 멈춰 개수를 셀 수 없고, 로그에 어떤 항목인지도 없다고 답했다. 전체를 건너뛰는 구조라 개수는 필요 없고 첫 오류만 남기면 된다고 판단했다. 대신 그 오류의 코드와 사유는 남기기로 하고, AI가 제시한 두 방식(VO data, Client가 숙소 코드 추가) 중 VO 방식을 골랐다.
- 질문: 공급사 에러 매핑을 `ErrorType`에서 다뤄야 하지 않는지, 하드코딩 매핑이 맞는지 물었다.
  - 처리: 수정
  - 이유: AI는 `ErrorType`(우리 API 응답)과 `SupplierFailureType`(내부 분류)의 쓰임이 달라 합치면 안 된다고 답했고, 이는 받아들였다. 매핑 형태는 `switch` 유지와 전용 enum 중 전용 enum을 골랐다.
- 질문: 외부 호출 구현부와 비즈니스 로직이 같은 도메인 패키지에 있어 경계가 이상하다고 했다.
  - 처리: 수정
  - 이유: AI는 implement 안 분리, 인터페이스만 도메인에 두는 의존성 역전, 통째로 최상위 분리 세 가지를 제시했다. 통째로 분리를 골랐고, AI가 입력 모델을 도메인에 두면 순환이 생긴다고 짚어 입력 모델까지 `external`로 옮겼다. Mock 패키지 이름도 바꿨다.

## Day 4 (2026-09-16)

### Mock 서버 모듈 분리와 공급사 패키지 이름 정리

**수행 내용**
- 공급사 Mock 서버를 루트 앱에서 떼어 별도 Gradle 모듈 `mock-supplier`로 옮겼다.
- 공급사 연동 패키지에서 `external` 한 단계를 빼 `com.trip.supplier`로 옮겼다.

#### **의사결정**

Mock 서버 모듈 분리

- 상황: Mock이 루트 앱의 `mock/supplier` 패키지에 있고 `@Profile("supplier")`로 켜졌다. 고정 JSON 두 개를 주는 서버인데, 띄우면 JPA·H2·`schema.sql`·스케줄러가 전부 올라왔다. 그래서 `application-supplier.yaml`이 실제 앱 기능을 끄는 파일이 돼 있었다(인메모리 DB로 교체, `stay.sync.enabled=false`). `architecture.md`도 `mock` 패키지만 계층·품질 규칙 예외로 두고 있었다.
- 채택: 별도 Gradle 모듈 `mock-supplier`. 자체 `@SpringBootApplication`과 `application.yaml`(9090)을 갖고 의존성은 webmvc 스타터 하나다. `@Profile`과 `application-supplier.yaml`은 없앴다.
  - 근거: 결정적이었던 건 Mock이 앞으로 자란다는 점이다(`decisions.md` Mock 항목: 모드 전환 `POST /control/{a|b}/mode`, 재고·요금 응답을 검색 구현 때 추가). 지금 구조는 Mock에 기능이 붙을 때마다 "실제 앱의 무엇을 더 꺼야 하는가"를 함께 관리해야 한다. 모듈을 나누면 끌 것이 없다. 운영 jar에서 Mock 코드가 빠지고, 규칙 예외 범위도 패키지가 아니라 모듈 경계와 일치한다.
- 비교: 현행 유지 — 드는 일이 없지만 위 비용이 계속 는다. 같은 모듈 + 별도 소스셋(`src/mock/java`) — 산출물 분리 효과는 같지만 Spring Boot 플러그인용 `BootRun`·`BootJar` 태스크를 손으로 등록해야 해 읽는 사람이 더 헤맨다.
- 판단: 얻는 게 같으면 표준적인 쪽이 낫다고 보고 모듈 분리를 택했다. 대가로 빌드 스크립트가 하나 늘고, 실행 명령이 프로파일 인자에서 `./gradlew :mock-supplier:bootRun`으로 바뀐다.
- 영향: `decisions.md`의 Build(단일 모듈 → 멀티 모듈)와 Mock 두 줄, `architecture.md` 패키지 트리·mock 섹션, `forbidden.md`, `review.md` 체크리스트, `CLAUDE.md` 명령어·구조를 고쳤다. 신규 공급사 추가 절차 표의 Mock 행 경로도 바꿨다.

`external` 접두어 제거

- 상황: `external`을 붙인 이유 중 하나가 "Mock 서버 패키지 이름이 `supplier`라 실제 연동 코드와 헷갈린다"였다(Day 3 기록). Mock이 별도 모듈로 나가면서 그 충돌이 사라졌다.
- 채택: `com.trip.external.supplier` → `com.trip.supplier`. 파일 22개, import 56군데, 규칙 문서 6개를 함께 고쳤다.
  - 근거: 이름이 막고 있던 충돌이 없어졌으므로 `external`은 경계를 가리키는 말로만 남는다. 그 경계는 이미 "도메인 → `supplier` 한 방향" 규칙 문장과 `review.md` 탐지 명령이 지키고 있어, 패키지 한 단계를 더 쓸 이유가 없다.
- 비교: 유지 — 규칙 문장에 경계 성격이 이름으로 드러나고, 결제 게이트웨이 같은 다른 외부 연동이 붙으면 `external/` 아래 나란히 놓인다. 다만 그런 연동 계획은 지금 없다.
- 판단: 예정에 없는 확장을 근거로 패키지 한 단계를 남기지 않는다. 다른 외부 연동이 실제로 생기면 그때 `external/`을 만들고 옮긴다.
- 영향: 규칙 문장이 "도메인 → `supplier` 한 방향"으로 바뀌었다. 과거 JOURNAL 기록에 남은 `external/supplier` 경로는 그 시점의 결정이라 그대로 두고, 신규 공급사 추가 절차처럼 앞으로 볼 부분만 고쳤다.

**AI 활용**
- 질문: Mock 쪽을 따로 모듈로 파는 게 나은지 물었다.
  - 처리: 수용
  - 이유: AI는 현행 구조가 치르는 비용(앱 전체 컨텍스트 기동, 기능을 끄는 yaml, 운영 jar 포함, 규칙 예외)을 정리하고 선택지 셋을 트레이드오프 표로 냈다. 결정을 가르는 지점이 "Mock이 앞으로 자라는가"라고 짚었고, `decisions.md`에 모드 전환·재고·요금 추가가 이미 적혀 있어 분리를 택했다.
- 질문: 모듈을 나눴으면 패키지 이름에서 `external`도 뺄 수 있지 않냐고 물었다.
  - 처리: 거부
  - 이유: AI는 "Mock과의 혼동"이라는 근거가 사라진 것은 인정하면서도, `external`이 의존 방향 규칙의 이름 역할을 하고 다른 외부 연동이 붙을 자리라는 이유로 유지를 권했다. 예정에 없는 확장을 위해 패키지 단계를 남길 이유가 없다고 보고 제거를 지시했다.

### 공급사 패키지 역할별 분류와 예외 계층 통합

**수행 내용**
- `supplier` 루트에 평평하게 있던 11개 타입을 `vo/`·`exception/`·`infra/`로 나누고, `WebClientConfig`를 `config/supplier`로 옮겼다.
- `SupplierCallException`을 `AppException` 하위로 합치고 공급사 실패에 `E2000`대 `ErrorType`을 붙였다.
- 공급사 응답 형식 패키지 이름을 `dto/`에서 `response/`로 바꿨다.

#### **의사결정**

패키지 역할별 분류

- 상황: `supplier` 루트 11개가 계약·입력 모델·실패 표현·실행 인프라 네 성격으로 섞여 있고, 구분은 `Supplier*` 접두어뿐이었다. 밖에서 실제로 import하는 건 5개고 나머지 6개는 내부 전용인데 그 경계가 드러나지 않았다. 검색 구현에서 `fetchOffers()`와 요금·재고 모델이 붙으면 더 는다.
- 채택: 루트에 계약(`SupplierClient`·`Supplier`)만 남기고 `vo/`(입력 모델), `exception/`(실패 표현), `infra/`(실행 인프라)로 나눴다. 설정 클래스인 `WebClientConfig`는 `com.trip.config.supplier`로 옮겨 `SwaggerConfig`와 나란히 뒀다.
  - 근거: 루트에 남은 것이 곧 도메인이 써도 되는 공개 면이 된다. 경계를 문장이 아니라 패키지로 드러낸다.
- 비교: 현행 유지 — 11개는 아직 한 화면에 들어오지만 검색 구현 때 어차피 나눠야 한다. `exception/`만 분리 — 이동은 가장 작지만 루트에 8개가 남아 성격이 다시 섞인다.
- 판단: 이름은 `failure/`·`http/` 대신 `exception/`·`infra/`로 정했다. `SupplierProperties`·`SupplierEndpoint`는 설정값이지만 실행기와 함께 쓰이므로 `infra/`에 두고, 빈 등록만 `config/`로 뺐다.

예외 계층 통합

- 상황: 같은 패키지 안에서 호출 실패는 독립 계층(`SupplierCallException extends RuntimeException`), 항목 검증 실패는 `AppException`으로 갈려 있었다. `StaySyncService`가 catch를 두 번 했고, `supplier`가 `support.exception`을 import하는 것도 그 때문이었다.
- 채택: `SupplierCallException extends AppException`. `SupplierFailureType`의 여섯 값이 각각 `ErrorType`(`SUPPLIER_BAD_REQUEST`~`SUPPLIER_MALFORMED`, `E2000`~`E2005`, 모두 502·WARN)을 들고, 생성자가 그것을 `super`로 넘긴다. `AppException`에는 `(ErrorType, data, cause)` 생성자를 더했다. 원인 예외와 원본 코드를 함께 보존할 생성자가 없었기 때문이다.
  - 근거: 공급사 실패도 결국 우리 API의 실패로 표현될 수 있어야 하고, 그 통로는 `ErrorType` 하나뿐이다.
- 비교: 실패 분류 전체에 `ErrorType` 하나만 두는 안 — enum 중복이 없는 대신 응답·로그에서 코드만으로는 원인이 갈리지 않는다. 별개 계층 유지 — catch가 둘로 남고 `INVALID_SUPPLIER_STAY`의 502는 나갈 경로가 없는 채로 남는다.
- 판단: 코드만 보고 원인이 갈리는 쪽을 택해 `SupplierFailureType`별로 `ErrorType`을 뒀다. 공급사 원본 코드는 `ErrorType`에 섞지 않고 `AppException`의 `data`에 `supplier type code` 문자열로 담는다. VO 검증 실패는 성격이 달라 `INVALID_SUPPLIER_STAY`로 남겼고, `StaySyncService`는 구체 예외를 먼저 잡는 순서를 유지한다.

응답 형식 패키지 이름

- 상황: `supplier/{a,b}/dto`에 든 8개가 전부 응답 파싱용이고 요청 형식은 하나도 없었다. 도메인 쪽은 이미 `controller/request`·`controller/response`로 나눠 쓰고 있어 관례가 프로젝트 안에서 두 갈래였다.
- 채택: `dto/` → `response/`.
  - 근거: 담긴 것이 전부 응답인데 이름만 무엇이든 담는 자루였다.
- 판단: 공급사 요청을 객체로 만들 일이 생기면 그때 `request/`를 만든다. 재고·요금 API는 넘길 파라미터가 적어 객체 없이 붙을 가능성이 높다.

**AI 활용**
- 질문: `supplier` 안이 분류가 없어 보이는데 예외 같은 건 `support`로 옮겨도 되지 않냐고 물었다.
  - 처리: 수정
  - 이유: AI는 `support`가 도메인을 모르는 공통 기반이라 `SupplierCallException`을 옮기면 `support → supplier` 역참조가 생긴다며 반대했고, 대신 같은 패키지 안에서 예외가 두 갈래인 것이 진짜 문제라고 짚었다. 그 진단은 받아들이되 패키지 이름은 AI가 낸 `failure/`·`http/` 대신 `exception/`·`infra/`로 바꿔 지시했고, `WebClientConfig`는 설정이니 `config`로 빼게 했다.
- 질문: 공급사 DTO가 도메인 계층으로 새어 나가고 있는 것 아니냐고 물었다.
  - 처리: 거부
  - 이유: AI가 리뷰 게이트의 탐지 명령으로 확인한 결과 도메인은 `SupplierStay` 같은 표준 모델만 참조하고 있었다. 다만 그 과정에서 `dto/` 안이 전부 응답 형식이라는 점이 드러나 패키지 이름만 `response/`로 바꿨다.
- 질문: AI가 제안한 코드 개선 넷 중 `call()`의 null 방어와 A·B Client 매핑 중복 제거만 골랐다.
  - 처리: 수정
  - 이유: null 방어는 `block()`이 빈 값을 주면 raw NPE가 스케줄러까지 올라가 남은 공급사 동기화까지 멈춘다는 근거가 있었다. 매핑 중복 제거는 변환을 응답 record로 내려 Client에 요청 조립과 판정만 남기는 쪽이 이미 정한 역할 분담과 맞았다. `maxOccupancy`의 null 처리와 주석·네이밍 정리는 근거가 취향에 가까워 두었다.

### #13 통합 검색 API

**수행 내용**
- 검색 API의 전체 흐름을 7단계(요청 검증 → 매핑 조회 → 코드 묶기 → 병렬 호출 → 정규화 → 결과 조립 → 응답)로 나누고, 단계마다 정할 항목을 분리했다.
- 요청 검증 규칙을 정하고 `StaySearchRequest`, `StayPeriod`, `Guests`와 `GET /api/v1/stays/search`를 구현했다.
- 응답·결과 VO(`SearchedRoom`, `SupplierFailure`, `StaySearchResult`)를 만들고, Service는 뼈대만 두었다.
- 검색 흐름의 이름을 room 계열로 통일하고, 입력 Command record와 출력 Response record를 모두 없앴다.
- `SupplierFailure`를 `supplier/vo`로 옮기고 `SupplierCallException.toFailure()`로 만들게 했다.
- 남은 구현을 6단계(매핑 조회 → Mock 재고·요금 → 공급사 Client·정규화 → 묶기·병렬 호출·결과 조립 → Mock 모드 전환 → 테스트)로 나누고, 단계마다 정할 항목을 분리했다.
- 검색용 매핑 조회(`StayManager.readMappings`, `RoomMapping`, 인원 조건 쿼리)를 구현했다.
- 공급사 호출 타임아웃을 세 구간으로 나누고 커넥션 풀을 공급사별로 분리했다.
- 기획안과 `decisions.md`를 전 항목 대조해 재고·요금 요청 단위를 잘못 이해한 것을 찾아 설계를 고쳤다.
- 조회 동사와 VO 이름 규칙을 `CLAUDE.md`에 정하고(`find`·`fetch`·`search`, `RoomOption` → `SearchedRoom`) 기존 이름을 맞췄다.
- 대조를 문서에서 기존 코드로 넓혀 B의 결과 코드 표와 검색 API 문서를 스펙에 맞췄다.
- Mock 재고·요금 응답과 모드 전환(`normal`/`error`/`no-response`)을 기획안 부록 뼈대대로 구현하고 네 모드를 직접 호출해 확인했다.
- 미분류로 남아 있던 중복 상품 병합을 확정으로 옮겼다(하지 않는다).
- 공급사 재고·요금 호출(`fetchRooms`)과 요금·재고 정규화를 A·B 양쪽에 구현하고, 띄워 둔 Mock에 실제로 붙여 값을 확인했다.
- `StayPeriod`·`Guests`를 `com.trip.support.vo`로 옮겨 `supplier → 도메인` 역방향 의존을 없앴다.
- 미결정으로 남아 있던 fan-out 코드 위치를 확정했다(`supplier/global`로 분리).
- fan-out 실행기 `SupplierDispatcher`와 검색 결과 조립(`RoomKey` 번역, `SearchedRoom.of`)을 구현해 검색 API를 끝까지 이었다.
- Mock을 띄워 정상·부분 실패·전부 실패·무응답 네 경로를 직접 호출해 확인했다.
- 세 타임아웃의 실패가 실제로 로그에서 갈리는지 `SupplierErrors.translate()`부터 로거까지 따라가 확인했다.
- `ApiControllerAdvice`와 `StaySyncService`가 원인 예외를 로거에 넘기도록 고쳤다.
- 연결 실패 코드 `CONNECTION`을 `CONNECT_TIMEOUT`·`DNS`·`CONNECT_REFUSED`로 쪼갰다.

#### **의사결정**

검색 조건 제약

- 상황: 날짜와 인원에 어떤 상한·하한을 둘지 정해야 했다.
- 채택: 체크인은 오늘 이후, 숙박은 최대 30박, 성인은 1명 이상, 아동은 생략 시 0.
  - 근거: 지난 날짜로는 예약할 수 없으니 과거 체크인은 받을 이유가 없다. 숙박일수 상한은 응답 크기와 직결된다. 재고·요금 응답은 숙소 수 × 객실 수 × 숙박일수로 커지는데 WebClient 기본 버퍼가 256KB라, 상한이 없으면 긴 기간 검색이 통째로 실패한다.
- 판단: 상한을 한 달로 잡았다. 숫자 근거는 없지만 검색 UI에서 흔히 쓰는 범위이고, 없는 것보다는 낫다고 봤다.

아동 기본값을 채우는 위치

- 상황: `children`이 없을 때 0으로 채우는 코드를 처음에 `StaySearchRequest.toCommand()`에 뒀다.
- 채택: `Guests` 생성자가 채운다.
  - 근거: "아동을 안 적으면 0명"은 변환이 아니라 해석이고, 그건 도메인 규칙이다. VO가 들고 있어야 다른 진입점에서 `Guests`를 만들어도 같은 규칙이 적용된다. Request는 변환만 한다는 기존 규칙과도 맞다.
- 비교: Request에서 채우기 — `Guests`가 "아동 미지정"과 "아동 0명"을 구분할 수 있지만, 지금 그 구분이 필요한 곳이 없다.
- 판단: 역할 책임으로 갈랐다. AI는 Request 쪽을 유지하자고 했는데 근거가 약해 뒤집었다.

요청 검증을 어디에 둘지

- 상황: 기존 규칙은 "도메인 제약은 VO에서만, Request는 변환만"이었다. 그런데 성인 1명 이상 같은 제약은 실무에서 보통 Request에 Bean Validation으로 둔다.
- 채택: 둘 다 둔다. 필드 하나로 판단되는 제약(날짜 필수, 성인 1 이상, 아동 0 이상)은 Request에 `@Valid`로, 필드 간 규칙(체크아웃 순서, 과거 날짜, 30박 상한)과 불변식은 VO 생성자에.
  - 근거: 지키는 대상이 다르다. Request는 HTTP 계약이라 오류를 필드별로 한 번에 돌려주고 OpenAPI 스키마에 제약이 드러나 프론트가 백엔드 코드를 안 읽어도 된다. VO는 객체 불변식이라 스케줄러·테스트처럼 HTTP를 거치지 않는 경로를 막는다. 한쪽만 두면 각각 그 이점을 잃는다.
- 비교:
  - VO만 유지 — 규칙을 안 고쳐도 되지만 오류가 한 건씩만 가고 스키마에 제약이 안 보인다.
  - Request만 — 검증이 한 곳이지만 "VO 생성자 검증" 결정을 뒤집게 되고, 필드 간 규칙은 클래스 레벨 커스텀 제약이 필요해 "별도 Validator 금지" 규칙과 충돌한다.
- 판단: 어느 쪽을 골라도 필드 간 규칙은 VO에 남아서 검증이 완전히 한 곳에 모이지 않는다. 그렇다면 선을 "필드 하나짜리는 Request, 필드 간 규칙은 VO"로 긋는 게 자연스럽다고 봤다. 대가로 `patterns.md`·`forbidden.md`의 "Request에서 다시 검사 금지" 규칙을 고쳤다.

`@ParameterObject` 제거

- 상황: 쿼리 파라미터를 객체로 받는 데 springdoc의 `@ParameterObject`를 붙여 뒀다.
- 채택: `@Valid @ModelAttribute`만 쓴다.
  - 근거: 바인딩은 Spring의 `@ModelAttribute`가 하고 `@ParameterObject`는 Swagger 문서를 펼치는 역할만 한다. 역할이 섞여 보이는 애노테이션을 빼고 표준 Spring 애노테이션으로 의도를 드러냈다.
- 판단: 대가로 Swagger UI에서 파라미터가 `request` 하나로 그려져 이 API를 직접 호출해 볼 수 없다. 문서 편의보다 애노테이션이 적은 쪽을 택했다.

검색 결과 단위의 이름

- 상황: 검색 결과 한 건을 `StayOffer`로, 응답 필드를 `stays[]`로 두고 있었다. 담기는 건 "이 기간·이 인원으로 팔 수 있는 객실 1건 + 총액 + 잔여 수"인데, 이름은 숙소를 가리키고 원소는 객실이라 한 숙소에 객실이 3개면 `stays`에 같은 숙소가 3번 나온다.
- 채택: 도메인 VO `SearchedRoom`, 응답 record `SearchedRoomResponse`, 필드 `rooms[]`. 공급사 쪽도 `SupplierRoom`·`searchRooms()`로 맞춘다.
  - 근거: 단위가 객실이라는 사실을 이름에 박아 필드명과 원소가 어긋나지 않게 했다. 공급사 입력부터 응답까지 room 한 단어로 이어져 계층을 넘어도 같은 것을 가리킨다는 게 보인다.
- 비교:
  - `Offer` 유지 — 바꿀 게 없지만 숙소 단위인지 객실 단위인지 이름에서 안 읽힌다.
  - `RoomAvailability` — 업계 표준어(ARI)라 뜻은 정확하지만 길고 요금이 이름에 안 드러난다.
  - `RoomQuote` — 총액이 중심이라 맞지만 예약 직전 단계 느낌이라 검색 결과에는 무겁다.
  - `AvailableRoom`·`BookableRoom` — `availableRooms=0`도 응답에 포함하기로 이미 정해서 이름이 거짓말이 된다.
- 판단: 도메인 의미가 얕더라도 읽는 사람이 헷갈릴 일이 없는 쪽을 골랐다. `Room`은 `RoomType` 엔티티와 겹쳐서 수식어를 붙였다.

공급사 호출 이름을 `fetch`/`search`로 가름

- 상황: 숙소 목록은 `fetchStays()`인데 요금·재고도 `fetchOffers()`로 계획해 뒀다.
- 채택: 요금·재고는 `searchRooms(StayPeriod, 객실 코드 목록)`.
  - 근거: 둘은 성격이 다르다. 목록은 하루 한 번 받아 DB에 저장하는 정적 마스터고, 요금·재고는 매 요청 조건을 넘겨 실시간으로 받는다. 같은 `fetch`를 쓰면 호출 비용과 시점 차이가 이름에서 사라진다.
- 판단: `fetch`는 받아오는 것, `search`는 조건으로 찾는 것으로 갈랐다.

입력 Command record 제거

- 상황: `StaySearchRequest.toCommand()` → `StaySearchCommand(StayPeriod, Guests)` → `search(command)` 순이었다.
- 채택: `StaySearchCommand`를 지우고 `search(StayPeriod period, Guests guests)`로 VO를 그대로 넘긴다. Request는 `toPeriod()`·`toGuests()`만 준다.
  - 근거: 필드 2개에 동작이 없고 호출부가 한 곳뿐이라 파라미터 목록의 별칭 이상이 아니었다. 게다가 출력은 `stay/vo/StaySearchResult`인데 입력만 `stay/business`에 있어 같은 유스케이스의 입출력이 서로 다른 패키지·이름 규칙을 따르고 있었다.
- 비교: `stay/vo/StaySearchCondition`으로 옮기고 개명 — 입출력이 둘 다 `vo`가 되어 대칭이 맞고 입력이 늘어도 시그니처가 안 변하지만, 지금은 감싸기만 하는 타입이 하나 남는다.
- 판단: 아직 입력이 둘뿐이라 없는 쪽을 택했다. 정렬·필터·통화처럼 입력이 붙어 시그니처가 길어지면 그때 `stay/vo`에 조건 VO를 만든다.

응답 record 제거

- 상황: `controller/response`에 `SearchedRoomResponse`·`SupplierFailureResponse`·`StaySearchResponse` 세 record가 있었다. `available`을 빼고 나니 셋 다 대응 VO와 필드가 완전히 같아졌고, `from`은 값을 그대로 옮기기만 했다.
- 채택: 셋을 지우고 `ApiResponse.success(result)`에 `StaySearchResult`를 그대로 넣는다. `controller/response` 패키지도 없앴다. 응답 JSON은 그대로다.
  - 근거: 입력 Command를 지운 것과 같은 이유다. 계층 경계라는 이름이 붙어 있었지만 실제로 하는 일이 없었다. 지금 `SupplierFailureType` 같은 내부 enum도 응답 record를 거쳐 그대로 직렬화되고 있어서, 경계가 막고 있는 것도 없었다.
- 비교:
  - 전부 유지 — VO 필드 변경이 API로 새는 걸 막지만, 막을 변경이 아직 없는데 record 3개와 `from` 3개를 계속 들고 있어야 한다.
  - 유지하고 일을 주기 — `SupplierFailureType`을 외부용 문자열로 번역시키는 안. 응답 record에 존재 이유가 생기지만, 그 번역이 지금 필요한지가 먼저다.
- 판단: 지금 하는 일이 없는 타입은 지우고, 응답 모양이 VO와 갈라지는 시점(내부 enum 번역·파생 필드·`@Schema`)에 다시 만들기로 했다. 대가로 VO 필드를 바꾸면 그게 곧 API 계약 변경이라서, VO를 고칠 때 응답 스펙을 같이 봐야 한다. `architecture.md`에 "`controller/response`는 응답 모양이 VO와 다를 때만 만든다"로 규칙을 적었다.

실패를 예외로 올리지 않고 응답 필드에 담는 이유

- 상황: `SupplierHttpCaller`·`SupplierErrors`가 이미 실패를 `SupplierCallException`으로 만들어 던지는데, 왜 응답에 `failures[]`를 또 두느냐는 물음이 있었다. HTTP 상태로 잡으면 되지 않나.
- 채택: 공급사 단위 실패는 잡아서 200 본문의 `failures[]`에 담는다. 예외는 지금처럼 그대로 던지고, 잡는 쪽이 경로에 따라 다르게 소비한다.
  - 근거: 검색은 공급사 N개를 병렬 호출해 합치는 fan-out이라 실패 단위가 요청 전체가 아니라 공급사 하나다. A가 180건을 정상으로 줬는데 B가 503이라고 예외를 올리면 A의 180건까지 같이 버려져서, 팔 수 있는 객실이 있는데 사용자가 아무것도 못 본다. "전체 타임아웃 5초 안에서 한 공급사가 늦어도 나머지로 응답한다"는 타임아웃 결정과도 부딪힌다. 그대로 올리면 공급사 하나가 느릴 때마다 항상 502가 나서 전체 타임아웃을 나눠 쓰는 설계가 무의미해진다.
- 비교: 207 Multi-Status — 일부 성공을 상태 코드로 표현할 수 있지만 WebDAV용이고 일반 REST 클라이언트가 해석하지 못한다.
- 판단: HTTP 상태는 응답당 하나뿐이라 "일부 성공"을 담을 수 없다. 그래서 상태 코드는 요청 전체의 성패만 말하게 두고, 공급사별 성패는 본문이 말하게 갈랐다. 정리하면 같은 `SupplierCallException`을 동기화는 잡아서 로그만 남기고, 검색은 잡아서 값으로 바꾸고, 요청 검증 같은 나머지는 안 잡아 `ApiControllerAdvice`로 보낸다.

공급사 전부 실패 시 응답

- 상황: 부분 실패 규칙만 있고 전부 실패하는 경우가 안 정해져 있었다. 그대로 두면 `rooms: []` + `failures: [A, B]`로 200이 나간다.
- 채택: 예외를 올려 502를 낸다.
  - 근거: 성공한 공급사가 하나도 없으면 부분 성공이 아니다. 200으로 주면 "검색은 됐는데 조건에 맞는 객실이 0건"과 본문을 열어보기 전까지 구별되지 않는다. 둘은 클라이언트가 다르게 다뤄야 하는 상황이다.
- 채택(이어서): 어떤 공급사가 실패했는지도 그 응답에 같이 담는다. `AppException(ErrorType.SUPPLIER_ALL_FAILED, failures)`로 던져 `List<SupplierFailure>`를 `ErrorMessage.data`에 싣는다(`E2006`, 502, WARN).
  - 근거: 502만 주면 클라이언트가 어느 공급사 때문인지 알 수 없어 재시도 판단도 장애 보고도 못 한다. `ApiResponse.error(ErrorType, Object)` → `ErrorMessage.data` 경로가 이미 있고, 검증 실패가 `{필드: 사유}` Map을 거기 담는 선례도 있다.
- 비교: 첫 실패를 다시 던지기 — 새 `ErrorType` 없이 끝나지만 `SupplierCallException` 생성자가 `data`를 `supplier type code` 한 건으로 고정해서 공급사 하나만 실린다. 목록을 담으려면 결국 생성자를 손대야 한다.
- 판단: 전용 `ErrorType`을 하나 늘리는 대가로 실패 목록을 구조체로 내보낸다. `SupplierFailureType`에는 대응 값을 두지 않았다. 이건 공급사 하나의 실패 분류가 아니라 검색 Service가 집계해서 내리는 판정이라 성격이 다르다.

`SupplierFailure`를 `supplier/vo`로 옮기고 `toFailure()` 추가

- 상황: `stay/vo/SupplierFailure(supplier, type, code)`가 `SupplierCallException`의 필드 세 개와 완전히 같았다. Service를 구현하면 catch 자리에서 `new SupplierFailure(e.getSupplier(), e.getType(), e.getCode())`를 손으로 조립하게 돼 있었다.
- 채택: `supplier/vo/SupplierFailure`로 옮기고 `SupplierCallException.toFailure()`가 만들게 했다.
  - 근거: 예외가 이미 완성된 값을 들고 있으니 도메인에서 같은 모양을 다시 선언할 이유가 없다. 게다가 이 record는 `stay/vo`에 있으면서 필드 타입 두 개(`Supplier`, `SupplierFailureType`)를 전부 `com.trip.supplier`에서 import하고 있었다. `SupplierStay`·`SupplierRoomType`을 `supplier/vo`로 옮겼던 것과 같은 성격의 타입이 도메인에 혼자 남아 있었다.
- 비교: `SupplierFailure`를 없애고 `failures[]`에 예외를 그대로 담기 — 타입이 하나 줄지만, `StaySearchResult`가 이제 API 응답 본문이라 `Throwable`의 `stackTrace`·`cause`·`suppressed`가 그대로 직렬화된다.
- 판단: 변환을 예외 쪽 메서드 하나로 모아 catch 자리마다 조립하지 않게 했다.

`SearchedRoom`에 `Supplier` 접두어를 붙이지 않음

- 상황: 공급사에서 가져온 응답 객체임을 드러내게 `SupplierSearchedRoom`으로 하자는 안이 나왔다.
- 채택: `SearchedRoom` 그대로 둔다.
  - 근거: 이건 공급사 응답 객체가 아니라 DB 절반 + 공급사 절반의 병합 결과다. "고정값 출처" 결정 때문에 `stayName`·`roomTypeName`·`maxOccupancy`는 DB 스냅샷에서 오고 재고·요금 API가 주는 같은 값은 무시한다. `stayId`·`roomTypeId`는 아예 우리가 발급한 UUID라 공급사가 본 적도 없다. 10개 중 5개에 잘못된 출처 표시를 다는 셈이다.
- 비교: 접두어를 붙이면 `supplier/vo/SupplierRoom`(`searchRooms()`가 주는 순수 공급사 응답)과 나란히 `Supplier*`가 둘이 되어, 접두어가 "공급사 패키지의, 공급사 API가 준 것"이라는 지금 뜻을 잃는다.
- 판단: 공급사에서 온 값이라는 표시는 `SupplierRoom`이 맡고, 병합 결과는 중립적인 이름으로 둔다.

응답에서 파생 필드 `available` 제거

- 상황: 응답에 `availableRooms`와 `available`(= `availableRooms > 0`)이 같이 있었다.
- 채택: `availableRooms`만 둔다.
  - 근거: 같은 값을 두 번 주는 것이고, 둘이 어긋난 응답이 나가면 어느 쪽이 맞는지 계약에 없다. 0인지 보는 판단은 클라이언트가 한다.
- 판단: 편의 필드 하나보다 계약에 모순이 생길 여지를 없애는 쪽이 낫다고 봤다.

인원 조건을 DB 조회에서 거른다

- 상황: 요청의 인원(`Guests`)을 받아만 두고 어디에 쓰는지 정하지 않았다. 공급사 스펙은 요청 인원을 수용할 수 있는 객실 타입만 반환한다(`adults + children <= maxOccupancy`).
- 채택: 매핑 조회 쿼리 조건으로 거른다(`findAllByStayIdInAndMaxOccupancyGreaterThanEqual`).
  - 근거: 공급사가 같은 기준으로 걸러 어차피 안 줄 코드를 요청에 실을 이유가 없다. 요청 코드 수가 줄면 묶음 수도 줄어 호출 횟수와 전체 타임아웃 소모가 같이 준다. 거르는 기준과 응답에 내려주는 `maxOccupancy`가 둘 다 DB 스냅샷이라 값이 어긋나지 않는다.
- 비교: 다 읽고 메모리에서 거르기 — 쿼리는 하나로 유지되지만 쓰지 않을 행을 DB에서 읽는다. 공급사 응답을 받은 뒤 거르기 — 호출 크기가 그대로다.
- 판단: 인원은 "보여줄 값"이 아니라 "조회 조건"이라고 보고 가장 앞 단계에서 적용했다.

객실 코드 묶음을 일급 컬렉션으로

- 상황: `searchRooms`에 넘길 객실 코드 목록의 타입이 없었다. 공급사는 (숙소 코드, 객실 코드) 쌍으로 객실을 식별한다.
- 채택: `supplier/vo/SupplierRoomCodes` — `Map<숙소코드, List<객실코드>>`를 감싼 일급 컬렉션.
  - 근거: `Map<String, List<String>>`을 그대로 넘기면 어느 String이 숙소이고 어느 쪽이 객실인지 시그니처에서 안 읽히고, 빈 값 검사가 호출부마다 흩어진다. `SupplierStay`·`SupplierRoomType`과 접두어 규칙도 맞다.
- 판단: 감싸는 김에 코드 검증과 `stayCodes()` 같은 동작을 타입 안으로 모은다.

묶음 호출은 공급사 간 병렬, 공급사 안 순차

- 상황: 공급사 재고·요금 API가 한 요청에 객실 코드를 50개까지만 받는다. 매핑된 객실이 그보다 많으면 한 공급사를 여러 번 불러야 한다.
- 채택: 공급사끼리는 가상 스레드로 병렬, 한 공급사 안의 묶음은 순차.
  - 근거: 실패 단위가 공급사이므로 "한 곳이 늦어도 나머지로 5초 안에 응답"은 공급사 간 병렬만으로 지켜진다. 동시 호출 수를 공급사 수만큼으로 묶어 한 공급사에 한꺼번에 몰리는 부하를 제한한다.
- 비교: 묶음까지 병렬 — 전체 타임아웃 안에 들 확률은 가장 높지만 동시 호출이 공급사 수 × 묶음 수로 늘어 커넥션 풀 크기와 공급사 rate limit에 바로 부딪친다.
- 판단: 대가로 묶음이 많으면 전체 타임아웃 5초를 넘길 수 있다. 그 대가는 아래 두 결정으로 받는다.

묶음 하나가 실패했을 때 뒤 묶음을 부를지

- 상황: 순차 호출이라 앞 묶음이 실패했을 때 뒤 묶음을 계속 부를지 정해야 했다.
- 채택: 실패 종류로 가른다. `AUTH`·`RATE_LIMITED`·`UNAVAILABLE`은 즉시 중단, `BAD_REQUEST`·`MALFORMED`·`INTERNAL`은 다음 묶음을 계속 부른다. 어느 쪽이든 앞서 받은 묶음의 객실은 살리고 `failures[]`에는 공급사 한 건만 남긴다.
  - 근거: 앞의 셋은 공급사 전체 상태를 뜻해 다음 묶음도 같은 결과가 나온다. 확실한 실패에 남은 시간을 태우지 않고 응답을 앞당기는 편이 낫다. 뒤의 셋은 그 묶음에 국한된 문제라 코드 하나 때문에 나머지 100개를 버릴 이유가 없다.
- 비교: 무조건 계속 — 규칙은 하나지만 공급사가 죽어 있으면 3초 타임아웃을 묶음 수만큼 반복해 응답이 항상 전체 타임아웃을 채운다. 즉시 중단 — 빠르지만 코드 하나의 `BAD_REQUEST`로 뒤 묶음을 시도조차 못 한다.
- 판단: 실패 분류를 "공급사 상태" 대 "요청 하나의 문제"로 나누는 축인데, 재시도 대상을 가를 때 쓸 축과 같다. 나중에 같은 분류를 그대로 쓴다.

전체 타임아웃 초과로 취소돼도 앞 묶음은 살린다

- 상황: `invokeAll(tasks, 5초)`는 전체 타임아웃을 넘긴 태스크를 취소한다. 태스크가 마지막에 결과를 `return`하는 구조면 취소된 `Future`에서 `CancellationException`만 나와, 그 공급사가 앞서 성공시킨 묶음까지 통째로 사라진다. "성공한 묶음의 상품은 그대로 응답한다"는 기존 결정과 어긋난다.
- 채택: 태스크가 묶음 하나를 받을 때마다 공유 수집기에 바로 쌓는다.
  - 근거: 결과가 반환값 경로에 의존하지 않으므로 어느 시점에 취소돼도 이미 들어간 객실은 남는다.
- 비교: 태스크가 마감 시각을 직접 보고 다음 묶음을 시작하지 않는 방식 — 반환값 경로가 하나로 유지돼 흐름이 눈에 잘 들어오지만, 남은 시간을 계산해 호출에 넘기는 코드가 붙고 마지막 호출이 마감을 넘기면 같은 문제가 남는다.
- 판단: 출처를 따로 추적할 필요는 없다. `SearchedRoom`이 `supplier` 필드를 들고 있어 공유 큐에 값만 쌓아도 어느 공급사 것인지 값 안에 붙어 온다. 따로 둘 것은 `failures` 수집기 하나뿐이다.

검색용 매핑 VO를 평면 1건으로

- 상황: 매핑 조회 결과의 모양을 정해야 했다. 숙소와 객실을 각각 VO로 둘 수도 있었다.
- 채택: `stay/vo/RoomMapping` 하나에 (stayId, stayCode, stayName, supplier, roomTypeId, roomTypeCode, roomTypeName, maxOccupancy)를 평면으로 담는다.
  - 근거: 쓰는 쪽이 전부 (숙소 코드, 객실 코드) 쌍 단위다. 공급사별로 묶을 때도, 응답을 조립하며 코드를 내부 식별자로 되돌릴 때도 그 쌍이 열쇠라 두 개로 나누면 쓸 때마다 다시 합쳐야 한다.
- 판단: `RoomTypeManager.readByStayIds`는 VO가 아니라 엔티티를 돌려준다. `RoomMapping`은 숙소 정보까지 있어야 만들 수 있는데 그 Manager는 `Stay`를 모르기 때문이다. 엔티티가 `implement` 밖으로 나가지는 않고, `StayManager.sync`가 `RoomTypeManager.sync`를 부르는 기존 협력과 같은 모양이라 그대로 뒀다.

검색 결과 정렬은 구현하지 않음

- 상황: 정렬·페이징은 범위 밖으로 뒀는데, 그러면 `rooms[]` 순서가 공급사 응답이 도착한 순서라 호출마다 달라진다.
- 채택: 정렬하지 않고, 순서가 보장되지 않는다는 것만 Swagger `description`에 적는다.
  - 근거: 정렬 기준(총액·잔여 수·숙소명)은 화면이 정할 문제고, 서버가 하나를 고르면 다른 기준이 필요할 때 계약을 바꿔야 한다.
- 판단: 대신 순서를 단정하는 테스트를 쓰지 않는다.

타임아웃을 세 구간으로 나누고 커넥션 풀을 공급사별로 둔다

- 상황: 타임아웃이 연결 1초, 응답 3초 두 개뿐이었다. 풀에서 커넥션을 기다리는 시간은 설정이 없어 Reactor Netty 기본값 45초였고, 풀도 전역 하나를 A·B가 공유했다(크기 `max(코어, 8) × 2` = 16, 유휴 정리 없음).
- 채택: 풀 대기 200ms, 연결 1s, 응답 2s. 커넥션 풀은 공급사별 `ConnectionProvider`(크기 50, 유휴 30s).
  - 근거: 세 구간이 재는 대상이 다르다. 연결은 네트워크 왕복, 응답은 공급사 처리 속도, 풀 대기는 우리 쪽 포화다. 45초 풀 대기는 `Mono.timeout()`이 전 구간을 덮어 실제로 발현되지는 않았지만, 그래서 오히려 풀 고갈과 공급사 지연이 똑같이 `TIMEOUT`으로 보였다. 나눠야 원인이 구분되고 재시도 대상도 가를 수 있다.
  - 근거: 응답을 3초에서 2초로 줄인 것은 묶음 순차 호출 결정의 결과다. 한 공급사의 최악이 `연결 + 응답 × 묶음 수`라 3초면 묶음 2개부터 전체 타임아웃 5초를 넘긴다.
  - 근거: 풀을 나눈 것은 A가 커넥션을 다 썼을 때 B 호출까지 대기에 걸리는 걸 막기 위해서다. 공급사 하나의 문제가 다른 공급사로 번지면 "한 곳이 실패해도 나머지로 응답"이 깨진다.
- 비교: 풀을 아예 쓰지 않기(`ConnectionProvider.newConnection()`) — 대기 자체가 사라지지만 호출마다 TCP 핸드셰이크(HTTPS면 TLS까지) 왕복이 붙고 닫은 소켓이 TIME_WAIT로 쌓인다. 한 검색이 공급사 2곳 × 묶음 N번을 부르므로 5초 전체 타임아웃을 그대로 깎아먹는다.
- 판단: 풀 크기 50은 근거가 약한 잠정값이라 yml로 뺐다. 공급사당 동시 1커넥션이라 곧 동시에 처리할 검색 요청 수이고, 톰캣이 가상 스레드라 이 값이 사실상 검색 동시성 제한이 된다. 넘으면 200ms 뒤 `POOL_EXHAUSTED`로 떨어져 백프레셔가 된다. 기다려도 같은 실패라 나중에 재시도 대상에서는 뺀다.

재고·요금 요청의 묶음 단위를 객실에서 숙소로 바로잡았다

- 상황: 남은 구현을 설계하면서 공급사에 보낼 코드 묶음을 `Map<숙소 코드, List<객실 코드>>`로 잡고 50개 상한도 객실 코드 기준으로 적어 뒀다. 구현에 들어가기 전에 기획안을 다시 읽으며 `decisions.md`를 통째로 대조했다.
- 채택: 공급사에 보내는 것은 숙소 코드 집합뿐이고 상한 50도 숙소 기준이다. 인터페이스는 `fetchRooms(StayPeriod, Guests, SupplierStayCodes)`.
  - 근거: 재고·요금 API 요청에 객실 코드를 실을 자리가 아예 없다. 어떤 객실 타입을 돌려줄지는 공급사가 인원으로 직접 고르며, 그래서 인원이 필수 파라미터인데 내 시그니처에는 그게 빠져 있었다.
  - 근거: 상한을 넘겨도 잘라 주지 않고 요청 전체가 400이다. 분할은 우리 책임이고, 반대로 응답 건수에는 상한이 없다.
- 비교: 잘못 잡은 채로 진행 — 객실 코드 50개씩 자르면 한 묶음에 든 숙소가 50을 넘어 전 묶음이 400으로 실패하거나, 반대로 숙소는 몇 개인데 객실이 많아 쓸데없이 쪼갠다.
- 판단: 객실 타입 매핑이 필요한 이유를 요청용으로 착각한 게 원인이었다. 매핑은 응답의 공급사 코드를 내부 식별자로 되돌리는 번역용이다. 인원 조건 조회는 그대로 두되 근거를 "보낼 코드를 줄인다"에서 "모든 객실이 인원을 못 받는 숙소를 통째로 뺀다"로 바꿨다. 아직 코드가 없던 단계라 문서만 고쳐 끝났다.

객실 `active`는 검색 조회에서 보지 않는다

- 상황: 인원 조건 쿼리에 `active = true`를 넣어 뒀다. 묶음 단위를 바로잡고 나니 애초에 우리가 판단할 일이 아니었다.
- 채택: 활성 숙소만 읽고 객실은 인원 조건만 본다.
  - 근거: 요청에는 숙소 코드만 싣고 어떤 객실을 팔지는 공급사 응답이 정한다. 목록에서 빠진 객실이면 공급사가 응답에 주지 않아 저절로 빠지고, 우리가 미리 걸러 버리면 공급사가 실제로 파는 객실을 매핑 없는 객실로 떨어뜨린다.
- 판단: 우리가 정하는 조건과 공급사가 정하는 조건을 한 쿼리에 섞어 뒀던 것이다. 우리 몫은 어느 숙소를 물어볼지까지다.

조회 동사를 신선도가 아니라 데이터 출처로 가른다

- 상황: 공급사 호출 이름을 정적 마스터 `fetchStays`와 실시간 `searchRooms`로 나눠 뒀다. 이름이 값의 신선도를 나타내고 있었다.
- 채택: `find`는 우리 DB, `fetch`는 공급사 호출, `search`는 우리 서비스의 도메인 동사. `searchRooms` → `fetchRooms`, `readMappings` → `findActiveRooms`, `readByStayIds` → `findByStayIds`.
  - 근거: 이름을 보고 알아야 하는 건 이 호출이 네트워크를 타는지다. 타임아웃·부분 실패·재시도가 붙는 자리가 거기다. 신선도는 호출 주기 설정이 말해 준다.
- 판단: 규칙을 `CLAUDE.md`에 적어 다음 도메인에도 같게 적용되게 했다. VO 이름도 같은 기준으로 파이프라인 단계를 드러내게 바꿨다(`RoomOption` → `SupplierRoom` → `SearchedRoom`).

Mock 재고·요금은 요청 날짜를 그대로 돌려준다

- 상황: Mock 재고·요금을 기획안 예시대로 고정 날짜 응답으로 두려고 적어 뒀다. `StayPeriod`가 과거 체크인을 막으므로 그 날짜로는 검색을 부를 수조차 없다.
- 채택: 요청 `checkIn`~`checkOut`을 읽어 그 기간의 날짜로 날짜별 요금·재고를 만든다. 숙소 코드와 인원은 계속 무시한다.
  - 근거: 미래 날짜로 부르면 이번엔 응답에 요청 기간의 날짜가 없어 정규화가 전 항목을 버린다. 어느 쪽이든 연동이 도는지 확인할 수 없다.
- 비교: 과거 체크인 제약을 푸는 안 — 기획안에 없는 우리 규칙이지만 지난 날짜로는 예약할 수 없다는 게 맞다. 검증 편의 때문에 도메인 규칙을 무를 이유가 없다.
- 판단: 고정 응답으로 두려던 건 Mock이 채점 대상이 아니라는 말에 기댄 것인데, 그 편의가 정작 검증을 막고 있었다. 날짜별 잔여 수에 0인 날을 하나 섞어 예약 불가 경로도 같이 본다.

예약 불가 상품 노출을 확정으로 옮겼다

- 상황: `availableRooms=0` 노출이 확정 표와 미결정 목록 양쪽에 걸쳐 있었다.
- 채택: 0으로 노출하고 빼지 않는다. 조건에 맞는 객실이 하나도 없어도 빈 목록으로 200을 준다.
  - 근거: "그 숙소가 없다"와 "있는데 그 날짜에 안 남았다"는 고객에게 다른 정보다. 결과 0건은 실패가 아니므로 응답 구조를 바꿀 일도 아니다.
- 비교: 별도 boolean 필드 — `availableRooms > 0`과 같은 값을 두 번 주고, 둘이 어긋났을 때 어느 쪽이 맞는지 계약에 없다.
- 판단: 응답 구조는 그대로 두고 값으로만 표현한다.

Mock은 기획안 부록의 뼈대를 그대로 따른다

- 상황: Mock 재고·요금을 어떻게 만들지 정하려는데 기획안 부록에 복사해 쓰라고 실어 둔 최소 뼈대가 있었다.
- 채택: 뼈대를 그대로 따르고 날짜만 요청 기간으로 바꾼다. 모드는 재고·요금에만 걸고 목록은 항상 정상, `no-response`는 600초를 잔다.
- 근거: Mock은 채점 대상이 아니고 부록이 "뼈대를 복사해 써도 된다"고 명시한다. 여기서 설계를 새로 하면 채점되지 않는 코드에 시간을 쓰는 것이다. 뼈대가 정해 준 값(무응답 600초, 모드 적용 범위, 매진 객실이 셋 중 하나)이 내가 따로 물어보려던 것들의 답이기도 했다.
- 비교: 내 판단으로 구조를 짜는 안 — 부록과 다른 형태가 되면 채점자가 스펙 준수 여부를 다시 읽어야 하고, 얻는 게 없다.
- 판단: 벗어난 것은 날짜 하나다. 예시의 `2026-09-01~03`은 오늘 기준 과거라 `StayPeriod`가 막고, 미래로 옮겨도 요청 기간과 어긋나면 정규화가 전 항목을 버린다. 잔여·요금 값은 예시 그대로 두되 숙박일 수가 3박이 아니면 돌려 쓴다.

중복 상품 병합은 하지 않는다

- 상황: 중복 상품 병합이 확정 표에도 미결정 목록에도 없었다. Mock 데이터에서 A의 `A-10023`과 B의 `B77120`이 같은 숙소라 검색 결과가 두 줄로 나온다.
- 채택: 합치지 않고 `supplier`가 다른 두 건으로 따로 노출한다.
- 근거: 동일 숙소를 알려주는 공통 키가 없어 숙소명으로 추정해야 하는데, 추정이 틀리는 두 방향의 무게가 다르다. 못 합치면 목록이 길어질 뿐이지만 다른 숙소를 합치면 엉뚱한 숙소의 재고·요금을 보여주고 예약까지 가면 사고다. 게다가 두 줄은 조건이 다르다 — A는 조식 미포함 429,000원, B는 조식 포함 452,000원이라 어느 쪽을 버려도 고객이 비교할 선택지가 하나 없어진다.
- 비교: 먼저 응답한 공급사를 남기는 안 — 같은 요청이 매번 다른 답을 주고, `invokeAll`이 제출 순서대로 돌려주므로 도착 순서 정보 자체가 없다. 공급사 우선순위 고정 — 결정적이 되지만 조건 차이 손실은 그대로다. 숙소 단위로 묶고 상품은 둘 다 노출 — 정보 손실은 없으나 응답 구조가 한 겹 깊어진다.
- 판단: 선택 구현이라 안 해도 감점이 없고, 기획안이 요구하는 건 병합 구현이 아니라 판단과 근거다. 대가는 같은 숙소가 두 줄로 보이는 것인데, 정렬을 넣지 않아 두 줄이 붙어 나오지도 않는다. 근거는 `README.md`에 남긴다.

`StayPeriod`·`Guests`를 중립 패키지로 옮겼다

- 상황: 3단계를 시작하려고 보니 `decisions.md`의 확정 두 개가 서로 어긋나 있었다. 하나는 "`supplier`는 도메인을 import하지 않는다"(그 순환을 없애려고 `SupplierStay`를 `supplier/vo`로 옮겼다)이고, 다른 하나는 `fetchRooms(StayPeriod, Guests, SupplierStayCodes)`인데 두 VO가 `stay/vo`에 있었다.
- 채택: `StayPeriod`·`Guests`를 `com.trip.support.vo`로 옮기고 시그니처는 그대로 둔다.
  - 근거: 두 VO는 숙박 도메인에만 매인 규칙이 아니라 기간·인원이라는 일반 값이다. 옮기면 타입이 한 벌만 남고 순환도 사라진다.
- 비교:
  - `supplier/vo`에 조회 조건 record를 따로 두기 — 포트가 자기 입력 모델을 갖는 정석이고 `SupplierStay`를 옮긴 것과 같은 선례지만, 필드가 같은 타입이 두 벌이 되고 호출부마다 변환 코드가 붙는다.
  - 원시값(`LocalDate`·`int`)을 그대로 넘기기 — 새 타입이 없지만 파라미터가 5개로 늘고, 원시 `Set<String>`을 거부하고 `SupplierStayCodes`를 만든 이유와 정면으로 어긋난다.
  - 규칙을 완화하고 그냥 import — 코드는 제일 적지만 `stay/vo ⇄ supplier` 순환이 되살아난다.
- 판단: AI는 포트가 자기 입력 모델을 갖는 안을 권했는데, 같은 필드를 가진 record를 한 벌 더 두는 값이 변환 코드 값보다 크지 않다고 봤다. 대가로 숙박 어휘 두 개가 `support`로 나갔다.

fan-out 코드를 Service에서 뺐다

- 상황: 공급사 병렬 호출 코드를 `StaySearchService`에 둘지 `supplier/global`로 뺄지 정하지 않은 채 4단계 앞에 와 있었다.
- 채택: `supplier/global`에 따로 둔다.
  - 근거: Service에는 비즈니스 로직만 있어야 한다. fan-out이 하는 일은 Executor 생성과 정리, 태스크 제출, 전체 타임아웃 `invokeAll`, 취소 처리, 묶음 분할, 실패 종류에 따른 중단·계속, 동시성 컬렉션까지 전부 호출 기술이다. 한데 두면 "검색이란 무엇인가"와 "스레드·타임아웃·취소를 어떻게 다루는가"가 한 클래스에 섞인다.
  - 근거: 빼고 나면 Service가 후보 조회 → 번역 → 전부 실패면 502, 세 줄로 읽힌다.
- 비교: Service 안에 두기 — 파일이 하나로 끝나고 호출부도 하나뿐이라 지금은 충분하지만, 그 이유로 두면 계층 규칙이 "코드가 적을 때는 안 지켜도 된다"가 된다.
- 판단: "확장 대비 선제 추상화 금지"에 걸리는지 따져 봤는데, 재사용을 노린 분리가 아니라 계층 규칙을 지키는 분리라 성격이 다르다고 봤다. 자리는 `SupplierHttpCaller`와 같은 `supplier/global`이되 맡는 범위가 다르다 — 한쪽은 HTTP 한 번, 다른 쪽은 공급사 N곳 × 묶음 M개 전체다.

태스크가 결과를 돌려주지 않고 공유 수집기에 쌓는다

- 상황: 병렬 호출 결과를 어떻게 거둘지 보는데, 평범하게 `Callable`이 `List<SupplierRoom>`을 `return`하고 `Future.get()`으로 받는 방식이 전체 타임아웃 초과에서 깨졌다.
- 채택: 태스크 밖에 사는 동시성 컬렉션(`rooms`·`failures`)에 묶음마다 바로 쌓고, 태스크는 `Callable<Void>`가 된다.
  - 근거(안 했을 때): 공급사 A에 숙소가 300개면 묶음이 6개다. 4번째까지 성공한 뒤 5번째에서 5초를 넘기면 `invokeAll`이 태스크를 취소하고 `Future.get()`은 `CancellationException`만 준다. 태스크 안 지역 변수에 모아 둔 4묶음치가 `return` 문에 닿은 적이 없어 통째로 사라진다. "앞서 받은 묶음은 살린다"는 부분 실패 설계가 전체 타임아웃 초과에서만 안 지켜지는 구멍이 된다.
  - 근거(했을 때): 컬렉션이 밖에 있으니 취소는 태스크만 죽이고 쌓인 4묶음은 남는다. "한 공급사가 늦어도 나머지로 응답"이 "한 공급사의 뒷 묶음이 늦어도 앞 묶음은 살린다"까지 확장된다.
  - 근거(타입): A·B 태스크가 같은 컬렉션에 동시에 쓰고 `invokeAll` 후 요청 스레드가 읽는다. `ArrayList`면 동시 `add`에서 원소가 유실되거나 내부 배열이 깨져 `ConcurrentLinkedQueue`를 쓴다.
- 비교: 묶음마다 태스크를 따로 제출하기 — 취소돼도 다른 묶음의 `Future`는 멀쩡하니 `return` 방식을 그대로 쓸 수 있지만, 동시 호출이 공급사 수 × 묶음 수로 늘어 "공급사 간에만 병렬" 결정과 커넥션 풀·rate limit에 부딪친다.
- 판단: 대가는 태스크 시그니처가 `Callable<Void>`라 무엇을 만드는지가 반환 타입에 안 드러나는 것이다. `Future`는 값 통로가 아니라 취소·실패 판정에만 쓰는 것으로 역할을 좁혔다.

전부 실패 판정을 물어본 공급사 수로 한다

- 상황: "공급사가 전부 실패하면 502"를 코드로 옮기는데, 손쉬운 판정은 "상품이 0건인데 실패가 1건 이상"이었다.
- 채택: 물어본 공급사 수(`SupplierDispatchResult.dispatchedSuppliers`)와 실패 수를 비교한다.
  - 근거: 손쉬운 판정은 틀린 경우가 있다. A가 정상적으로 "조건에 맞는 객실 없음"(0건)을 주고 B만 실패하면 상품 0건 + 실패 1건이 되어 502가 나간다. 그건 부분 실패지 전부 실패가 아니다.
- 비교: Service가 공급사별 숙소 코드 맵의 키 개수와 비교 — 맵에는 있지만 Client 구현체가 없는 공급사가 DB에 남아 있으면 물어본 적 없는 공급사까지 분모에 들어간다. 실제로 누구를 불렀는지는 Dispatcher만 안다.
- 판단: 분모를 아는 쪽이 값을 같이 돌려주게 했다. 부른 곳이 하나도 없으면(`dispatchedSuppliers == 0`) 실패가 아니라 물어볼 게 없던 것이라 `allFailed()`가 거짓이다.

태스크 안에서 터진 예기치 못한 예외를 삼키지 않는다

- 상황: 공유 수집기 구조라 `Future.get()`을 부르지 않는다. 그러면 태스크에서 `SupplierCallException`이 아닌 `RuntimeException`이 터져도 아무 데도 안 남는다.
- 채택: `MALFORMED/UNEXPECTED` 실패로 남기고 `error` 로그를 찍은 뒤 그 공급사의 남은 묶음을 멈춘다.
  - 근거: 안 잡으면 그 공급사가 상품도 실패도 없이 사라진다. 그럼 전부 실패 판정이 실패 0건을 보고 200 + 빈 목록을 주어, 장애가 "조건에 맞는 객실 없음"으로 둔갑한다. 502를 내야 할 상황에서 200이 나가는 게 제일 나쁘다.
- 비교: `Future.get()`을 불러 `ExecutionException`으로 받기 — 표준 경로지만 취소된 태스크에서는 `CancellationException`만 나와 공유 수집기를 쓴 이유와 정면으로 부딪친다.
- 판단: 분류를 `MALFORMED`로 둔 건 `SupplierErrors`가 알 수 없는 원인에 이미 쓰는 값이라서다. 우리 쪽 버그라 다음 묶음도 같은 결과일 테니 계속하지 않는다.

번역 열쇠에 공급사를 넣었다

- 상황: 재고·요금 응답을 후보(`RoomOption`)로 되돌리려는데 `SupplierRoom`에 공급사 정보가 없었다. 숙소 코드와 객실 코드만으로 찾으면 된다고 봤다.
- 채택: `SupplierRoom`에 `supplier` 필드를 넣고 `stay/vo/RoomKey(supplier, stayCode, roomTypeCode)`로 찾는다.
  - 근거: 스펙이 "숙소 식별자는 Supplier A 안에서만 유일"이라고 못 박는다. 공급사가 다르면 같은 문자열이 다른 숙소일 수 있어, 공급사를 빼면 엉뚱한 후보에 요금이 붙는다. 중복 상품 병합을 하지 않기로 한 것과 같은 사실의 뒷면이다.
- 비교: 문자열을 이어 붙여 열쇠로 쓰기 — 타입이 없어 어떤 값이 몇 개 들어가는지 호출부마다 다시 읽어야 한다.
- 판단: `RoomKey.from(RoomOption)`·`from(SupplierRoom)` 두 정적 팩터리를 둬 양쪽에서 같은 열쇠가 나오게 했다. 열쇠 만드는 규칙이 한 곳에만 있다.

실패 원인 식별

- 상황: 타임아웃을 세 구간으로 나눠 뒀는데, 장애가 났을 때 로그만 보고 어느 구간에서 터졌는지 실제로 가려낼 수 있는지 확인이 필요했다. 분류 코드는 `POOL_EXHAUSTED`·`TIMEOUT`·`CONNECTION`으로 나뉘어 있었지만 거기까지였다.
- 채택: 로거에 원인 예외를 넘기는 것과 `CONNECTION` 분리 두 가지
  - 근거: 원인 예외는 `SupplierCallException`이 `cause`로 이미 들고 있는데 로거가 마지막 인자로 안 넘겨 SLF4J가 스택트레이스를 버리고 있었다. 만들어 둔 정보를 출력 직전에 버리는 상태라 인자 하나로 해결된다.
  - 근거: `CONNECTION`은 연결 타임아웃·connection refused·DNS 실패 셋을 한 코드로 뭉갠다. "상대가 느린 것"과 "상대가 죽은 것"은 대응이 달라 로그 한 줄로 갈려야 한다.
- 비교: 응답 소요 시간·호출 URI 기록, 커넥션 풀 메트릭 노출, 검색 경로 실패 로깅 — 셋 다 미채택.
- 판단: 앞의 둘은 수정 범위가 좁고 없으면 원인 판별 자체가 막히는 항목이라 먼저 했다. 나머지 셋은 원인 판별을 막는 게 아니라 정밀도를 올리는 쪽이고, 특히 검색 경로는 `StaySearchService`가 아직 뼈대라 붙일 자리가 없어 구현 뒤로 미뤘다. 실패 코드를 참조하는 기존 테스트가 없어 이번에는 컴파일만 확인했고, 분류 분기 테스트는 공급사 Client 테스트를 쓸 때 같이 붙인다.

`CONNECT_TIMEOUT`을 먼저 검사하는 순서

- 상황: 연결 실패 원인을 `cause` 체인에서 타입으로 가려내야 했다.
- 채택: `ConnectTimeoutException` → `UnknownHostException` → `ConnectException` 순서
  - 근거: Netty의 `ConnectTimeoutException`이 `java.net.ConnectException`의 하위 타입이라, `ConnectException`을 먼저 보면 연결 타임아웃이 전부 `CONNECT_REFUSED`로 빨려 들어간다.
- 판단: 풀 고갈 판정을 `TIMEOUT` 분기 앞으로 옮긴 것과 같은 이유다. 상속 관계가 있는 타입은 좁은 쪽을 먼저 본다는 규칙으로 맞췄다.

**막힌 점과 해결**
- 검증 실패 응답이 필드를 알려주지 않았다. `ApiControllerAdvice`가 E400 한 줄만 주고 어느 필드가 틀렸는지 없었다. `MethodArgumentNotValidException`의 필드 오류를 `{필드: 사유}`로 모아 `data`에 담게 고쳤다.
- 그랬더니 날짜 형식 오류에서 Spring 기본 메시지가 내부 타입과 어노테이션 목록을 응답에 그대로 흘렸다. `isBindingFailure()`인 경우만 "형식이 올바르지 않습니다"로 바꿨다.
- `@ModelAttribute` + record 조합의 예외 타입이 `BindException`일지 `MethodArgumentNotValidException`일지 확실치 않아 앱을 띄워 확인했다. 후자라 기존 핸들러가 그대로 받았다.
- 기획안 대조를 문서에서 코드로 넓혔더니 `BResultCode`에 B의 `E500`(공급사 내부 오류)이 빠져 있었다. 모르는 코드의 기본값이 `INTERNAL`이라 분류 결과는 같아서 그동안 드러나지 않았는데, 그러면 스펙에 있는 코드와 쓰레기 값을 구분할 수 없다. 표에 있는 코드는 전부 선언했다. 검색 API의 Swagger `description`도 내부 동작만 적혀 있어 부분 실패·빈 결과·정렬 없음을 계약으로 적었다.
- 풀 고갈을 따로 분류하려는데 `PoolAcquireTimeoutException`이 `TimeoutException`을 상속해서 기존 `TIMEOUT` 분기에 먼저 걸렸다. 판정 순서를 앞으로 옮기고, reactor-pool이 shaded 패키지로 들어 있어 타입 대신 클래스 이름으로 판별했다.
- 중복 상품 병합 결정이 Day 4 #13이 아니라 `JOURNAL.md` 맨 위 템플릿 주석 안에 들어가 있었다. 같은 모양의 빈 블록이 템플릿에도 있어서 붙는 자리를 잘못 잡은 것이다. 옮기면서 보니 앞의 Mock 결정도 제목 줄이 빠져 있어 같이 채웠다.
- 공급사에 보내는 `hotelCodes`는 쉼표로 이어 붙이는데, `queryParam`이 쉼표를 `%2C`로 인코딩하면 공급사가 코드 하나로 읽는다. 실제로 만들어지는 URI를 찍어 쉼표가 그대로인 것과 날짜가 ISO로 나가는 것을 확인했다.
- `TOTAL_TIMEOUT_EXCEEDED`(전체 타임아웃 5초 초과) 경로는 Mock으로 재현하지 못했다. 무응답 모드를 켜도 응답 타임아웃 2초가 먼저 걸려 `TIMEOUT`으로 떨어진다. 전체 타임아웃이 실제로 물려면 한 공급사에 묶음이 여러 개여야 하고(연결 1s + 응답 2s × 묶음 수 > 5s) 그러려면 숙소가 51개 이상이어야 하는데, Mock 목록은 세 건이다. 6단계 테스트에서 본다.
- `connectTimeout` 초과가 왜 `TIMEOUT`이 아니라 `CONNECTION`으로 떨어지는지 처음에 안 보였다. `ConnectTimeoutException`이 이름과 달리 `TimeoutException` 계열이 아니라 `ConnectException` 계열이라 기존 `hasCause(TimeoutException...)` 검사를 그냥 통과하고 있었다. 이름이 아니라 상속 계층을 봐야 했다. 풀 고갈 때와 같은 함정을 반대 방향으로 밟은 셈이다.

**AI 활용**
- 질문: 구현 전에 고려할 사항과 내가 판단할 항목을 물었다.
  - 처리: 수정
  - 이유: 확정·미결정·엣지 케이스가 한 번에 쏟아져 감이 안 잡혔다. 흐름을 단계로 자르고 단계마다 정할 것만 붙이게 다시 시켰다.
- 질문: 아동 기본값은 VO가 채우는 게 역할 책임상 맞지 않냐고 물었다.
  - 처리: 거부
  - 이유: AI는 "생략하면 0은 HTTP 요청의 규칙"이라며 Request 유지를 권했다. 그건 변환이 아니라 도메인 해석이라고 보고 VO로 옮기게 했다.
- 질문: `@ParameterObject`가 뭔지, `@RequestParam`과 같은데 객체 형태인 건지 물었다.
  - 처리: 수용
  - 이유: 바인딩은 `@ModelAttribute`가 하고 `@ParameterObject`는 springdoc 문서용이라 역할이 다르다는 설명을 받았다. 그래서 `@ModelAttribute`로 바꿨다.
- 질문: `SupplierFailureResponse`를 왜 따로 만들었고 기존 타입을 왜 안 쓰냐고 물었다.
  - 처리: 수정
  - 이유: AI가 `support/exception/ErrorMessage` 얘기로 읽고 그걸 안 쓴 이유부터 답했다. 내가 물은 건 `SupplierHttpCaller`가 이미 만들고 있는 예외 경로였다. 다시 짚어 주니 `SupplierCallException`이 (supplier, type, code)를 그대로 들고 있다는 걸 확인하고 `toFailure()`로 정리했다. 다만 그 과정에서 응답 record 셋이 전부 VO의 복사본이 됐다는 지적은 맞아서 같이 지웠다.
- 질문: `SearchedRoom`에 `Supplier` 접두어를 붙여 공급사 응답 객체임을 드러내자고 제안했다.
  - 처리: 거부
  - 이유: 필드 절반이 DB에서 오고 식별자는 우리 UUID라 접두어가 절반을 잘못 표시한다는 답을 받았다. `supplier/vo/SupplierRoom`과 뜻이 겹쳐 접두어 규칙이 무너진다는 점도 있었다.
- 질문: 실패를 HTTP 상태로 잡으면 되지 왜 응답 필드에 담느냐고 물었다.
  - 처리: 수용
  - 이유: 공급사 fan-out이라 일부만 실패할 수 있고, 예외를 올리면 성공한 공급사 결과까지 버려진다는 설명을 받았다. 물으면서 전부 실패하는 경우가 안 정해져 있다는 걸 알게 돼 502로 정했다.
- 질문: 전부 실패해서 502를 낼 때 어떤 공급사가 실패했는지도 응답에 담을 수 없냐고 물었다.
  - 처리: 수용
  - 이유: `ErrorMessage.data`가 이미 그 자리라는 답을 받았다. 덧붙여 첫 실패를 다시 던지는 안으로는 공급사 하나만 실린다는 걸 알게 돼서, 남겨뒀던 `ErrorType` 선택을 전용 값(`SUPPLIER_ALL_FAILED`)으로 정리했다.

- 질문: 요금·재고 검색의 이름이 직관적이지 않고, Command record 없이 VO를 그대로 쓰면 되지 않냐고 물었다.
  - 처리: 수정
  - 이유: Command는 지워도 된다는 답은 맞았지만, 결과 단위 이름으로 AI가 낸 후보가 전부 `Offer` 변형이었다. 다른 단어를 다시 요구해 `SearchedRoom`을 골랐다.
- 질문: 3단계로 넘어가겠다는 말만 하고 멈춰 있길래 구현한 거냐고 물었다.
  - 처리: 수정
  - 이유: AI가 다음 단계를 예고만 하고 끝내 버렸다. 다시 시키니 그제서야 착수했고, 그 과정에서 확정 두 개가 서로 어긋나는 걸 찾아 왔다. 예고와 착수를 한 턴에 붙이게 했다.
- 질문: `supplier`가 도메인을 import하지 않는 규칙과 `fetchRooms(StayPeriod, Guests, ...)` 시그니처가 충돌하는데 어느 쪽으로 맞출지 네 가지 안을 받았다.
  - 처리: 거부
  - 이유: AI 추천은 `supplier/vo`에 조회 조건 record를 새로 두는 안이었다. 필드가 같은 타입이 두 벌이 되는 게 마음에 걸려 `StayPeriod`·`Guests`를 `support/vo`로 옮기는 쪽을 골랐다.
- 질문: fan-out 코드가 뭐냐고 물었다.
  - 처리: 수용
  - 이유: 한 요청을 공급사 여러 곳에 뿌리고 다시 모으는 층이고, 3단계에서 만든 `fetchRooms`를 감싼다는 설명을 받았다. Service에는 비즈니스 로직만 둬야 하니 빼는 게 맞다고 보고 `supplier/global`로 정했다.
- 질문: 공유 수집기가 뭐고 무엇을 공유하는 거냐고 물었다.
  - 처리: 수용
  - 이유: `return` 방식이면 전체 타임아웃 초과로 취소될 때 이미 받아 둔 묶음까지 전부 사라진다는 설명을 받았다. 그건 말이 안 되는 동작이라 구조를 받아들이고, 왜 이 구조인지를 `decisions.md`에 문제·해결 순으로 남기게 했다.
- 질문: fan-out 실행기 이름 후보 셋(`SupplierFanOut`·`SupplierRoomGatherer`·`SupplierDispatcher`) 중에서 골랐다.
  - 처리: 수정
  - 이유: AI가 문서에 적어 둔 건 `SupplierFanOut`이었다. fan-out은 설명할 때나 쓰는 말이지 클래스 이름으로는 하는 일이 안 읽혀서 `SupplierDispatcher`로 바꿨다.
- 질문: 1 이상 같은 제약은 원래 실무에서 Request에 `@Valid`로 두지 않냐고 물었다.
  - 처리: 수정
  - 이유: AI가 처음에는 기존 규칙과 충돌한다는 점만 짚고 관례 자체는 답하지 않았다. 다시 물으니 둘을 같이 두는 게 실무 관례이고 중복이 아닌 이유(계약 대 불변식)를 설명했다. 그 기준으로 둘 다 채택하고 규칙 문서를 고치게 했다.
- 질문: 남은 개발 순서와 각 단계에서 고려할 것을 물었다.
  - 처리: 수용
  - 이유: 6단계로 자른 순서와 단계 간 선행 관계가 납득됐다. 다만 "50개 분할"은 AI가 이슈 체크리스트에서 가져왔을 뿐 근거를 모른다고 해서, 공급사 API의 요청당 코드 상한이라는 걸 내가 짚어 줬다.
- 질문: 묶음 하나가 실패하면 나머지도 못 받는 것인지, 앞 묶음이 실패하면 뒤 묶음은 못 부르는 것인지 물었다.
  - 처리: 수정
  - 이유: "성공한 묶음은 살린다"는 답은 기존 결정 그대로였는데, 전체 타임아웃 초과로 태스크가 취소되면 그 성공분까지 사라진다는 함정은 이 질문을 받고서야 나왔다. 선택지를 다시 받아 공유 수집기로 정했다.
- 질문: 코드 묶음을 Map으로 넘기되 일급 컬렉션으로 감싸는 건 어떤지 물었다.
  - 처리: 수용
  - 이유: 원시 `Map<String, List<String>>`은 두 String의 뜻이 시그니처에서 안 읽히고 빈 값 검사가 흩어진다는 답을 받았다. 이름만 `supplier/vo` 규칙에 맞춰 `SupplierRoomCodes`로 골랐다.
- 질문: 타임아웃을 풀 대기·연결·응답 셋으로 나눠야 하지 않냐고, 지금은 어떻게 되어 있는지 물었다.
  - 처리: 수용
  - 이유: 확인해 보니 실제로 둘뿐이었고 풀 대기는 기본값 45초였다. 세 구간이 재는 대상이 다르다는 설명과 값 선택 기준을 받아 정했다. 응답 타임아웃이 묶음 순차 호출과 안 맞는다는 것도 이때 드러나 2초로 줄였다.
- 질문: 트랜잭션 안에서 도는 것도 아닌데 공급사 호출에 풀을 왜 쓰냐고 물었다.
  - 처리: 수용
  - 이유: 여기 풀은 DB 커넥션 풀이 아니라 TCP 소켓 풀이라 트랜잭션과 무관하다는 답을 받았다. 매 호출 핸드셰이크 왕복과 TIME_WAIT 누적을 피하려는 것이고, Reactor Netty는 기본이 풀 사용이라 우리가 켠 게 아니라 안 끈 상태였다는 것도 확인했다.

- 질문: 기획안을 다시 읽고 `decisions.md`에 적은 항목이 기획안과 맞는지, 잘못 이해해 구현한 게 없는지 전부 대조하라고 시켰다.
  - 처리: 수용
  - 이유: 재고·요금 요청의 묶음 단위를 객실로 잡은 것과 인원 파라미터가 빠진 것을 찾아냈다. 근거로 든 요청 형식을 기획안에서 직접 확인하고 반영했다. 구현 전에 시켜서 문서만 고치면 끝났다.
- 질문: 50개 상한은 내가 정하지 않아도 알아서 지켜지는 것 아니냐고 물었다.
  - 처리: 수용
  - 이유: 초과하면 잘라 주는 게 아니라 요청 전체가 400이고, 공급사가 알아서 거르는 건 개수가 아니라 인원이라는 답을 받았다. 분할은 우리 책임이라는 게 분명해졌다.
- 질문: 객실 `active`를 검색 조회에서 보는 게 맞냐고 되물었다.
  - 처리: 수정
  - 이유: AI는 내가 쿼리에 넣어 둔 `active = true`를 그대로 문서에 옮겨 적고 되돌릴지 물어보기만 했다. 애초에 우리가 판단할 자리가 아니라는 걸 내가 짚어 조건을 빼게 했다.
- 질문: 외부 호출은 `fetch`, 우리 서비스 내부는 `search`로 통일하라고 했다.
  - 처리: 수용
  - 이유: 기존 `fetch`/`search` 구분이 신선도 기준이라 `CLAUDE.md`의 새 규칙과 충돌한다는 걸 AI가 먼저 짚었고, 어느 쪽을 택할지는 내가 정했다.
- 질문: 기획안에서 제공된 Mock 코드가 있지 않냐고 물었다.
  - 처리: 수정
  - 이유: AI가 Mock 데이터를 어떻게 구성할지 선택지를 만들어 물어봤는데, 부록에 복사해 쓸 뼈대가 이미 실려 있었다. 다시 찾아보게 하니 뼈대가 그 질문들의 답을 대부분 담고 있었다. 채점 대상이 아닌 코드에 판단을 들이지 않게 됐다.
- 질문: 중복 상품 병합을 먼저 성공적으로 응답한 공급사 기준으로 하자고 하고, 합치는 기준을 정해 둔 적이 있는지 물었다.
  - 처리: 수정
  - 이유: 정해 둔 적이 없다는 건 맞았는데, 도착 순서를 기준으로 삼으면 같은 요청이 매번 다른 답을 주고 `invokeAll`에는 그 순서 정보조차 없다는 반론을 받았다. 무엇을 같은 숙소로 볼지가 먼저라는 것도 짚었다. 따로 노출하면 조식 포함·미포함 가격이 둘 다 보인다는 걸 확인하고 병합을 넣지 않는 쪽으로 정했다.
- 질문: 풀 대기·연결·응답 세 타임아웃이 어떻게 다르고 비동기 구조에서 어떻게 동작하는지 물었다.
  - 처리: 수용
  - 이유: 요청 하나의 생애주기에서 각 타임아웃이 붙는 구간이 갈린다는 설명이 명확했다. 최악의 경우 총 소요가 세 값의 합이라는 것, `maxIdleTime`이 상대 서버의 idle timeout보다 짧아야 간헐적 connection reset을 피한다는 것을 새로 알았다.
- 질문: 지금 구조가 원인 구분을 위해 나눠 구현한 게 맞는지, 로그로 어느 지점인지 식별 가능한 상태인지 물었다.
  - 처리: 수정
  - 이유: 분류는 되지만 로그에서는 안 갈린다는 답과 근거(로거가 `cause`를 버림, `CONNECTION`이 셋을 뭉갬)는 맞아서 받았다. 다만 AI가 `block()` 때문에 스레드가 묶여 사실상 동기라고 지적한 건 틀렸다. 가상 스레드로 처리할 계획이고 `block()`은 스택트레이스를 얻으려고 일부러 쓴 것이라고 짚어 철회시켰다. 코드만 보고 설정(`spring.threads.virtual.enabled`)을 확인하지 않은 지적이었다.
- 질문: 지적된 다섯 가지 중 원인 예외 유실과 분류 해상도 둘만 고치라고 했다.
  - 처리: 수용
  - 이유: 나머지 셋은 지금 붙여도 확인할 대상이 없거나 정밀도 항목이라 미뤘다. AI가 개선안을 비용과 효과로 나눠 제시해 고르기 쉬웠다.
- 보류: `SupplierHttpCaller`의 Reactor `.timeout()`과 `WebClientConfig`의 Netty `responseTimeout`이 같은 값 2초로 이중으로 걸려 있다는 지적을 받았다. 이중화의 뜻은 "Netty는 응답 첫 바이트까지, Reactor는 본문까지"인데 값이 같으면 본문 스트리밍 보호 효과가 없다는 것이다. 이번 범위 밖이라 두었고 값 분리 여부는 나중에 정한다.
